# 設計書：creative-thread-pipeline（創作スレ台本 一気通貫スキル）

- 作成: 2026-06-20
- 起点: 平将門の祟り回の制作過程（創作スレ→YMM4台本）を再現可能なスキルに昇華する
- 状態: **設計確定（平将門の全工程と照合しレビュー反映済 → §13・実装待ち）**

---

## 1. 背景と目的
平将門回で「創作スレ（オリジナル2chスレ）→ 解説者パート → YMM4取り込み用CSV/xlsx」を作ったが、
**組み立て(アセンブル)とxlsx化・ビューアは使い捨てスクリプト(gen_csv.py / make_xlsx.py)で手組み**だった。
この工程を**毎回再現できるスキル**にする。生成系は既存スキルを呼び、本スキルは
**「組み立て規約 ＋ 検品ゲート ＋ 出荷物の生成」**を担う。

## 2. 既存スキルとの関係（重複回避が最重要）
| スキル | 役割 | 本スキルとの関係 |
|---|---|---|
| `youtube-pipeline` | **実スレscrape版**の6ステップ一気通貫（thread_scraper.pyで本物スレ取得） | 兄弟。creative版が本スキル。結合エンジンは共有化を狙う |
| `creative-thread-gen` | 創作スレ（前半/後半）生成＋qa_check（16+ゲート） | **STEP2で呼ぶ**（×2本）|
| `youtube-script-parts` | 冒頭・解説・前置き・締め生成 | **STEP3で呼ぶ**（解説は対象用語ぶん）|
| `youtube-script-checker` | 規約3段階チェック | **STEP5で呼ぶ** |
| `ymm4-dic-generator` | 読み仮名dic（毎回書き換え・漏れ/誤読ゼロ） | **STEP6で呼ぶ** |
| `ymm4-pronunciation-checker` | YMM4取り込み後の発音照合 | 任意・STEP7（dic適用後）|

→ **本スキルは生成を再実装しない。オーケストレーション＋アセンブル＋出荷に徹する。**
→ アセンブル規約は `scripts/assemble_csv.py` ＋ `refs/組み立て規約.md` に集約し、
   将来 youtube-pipeline もこの共有エンジンを採用して規約の二重化を防ぐ。

## 3. スキル定義
- **名前**: `creative-thread-pipeline`
- **トリガー**: 「創作スレ台本」「オリジナルスレで台本」「スレ台本一気通貫」「転載なしで台本」「creative pipeline」
- **立ち位置**: ネタ確定後〜YMM4取り込み直前までの創作スレ版フルパイプライン
- **各ステップ後にユーザー確認**（youtube-pipeline踏襲・暴走防止）

## 4. パイプライン全体
```
STEP1 ネタ確定（テーマ＋前半=タイトルネタ/後半の振り分け）
      ※ネタ出しは neta-research / thread-research-planner（任意・前段）
STEP2 創作スレ生成 ×2  →  /creative-thread-gen（前半・後半）
      ・品質バー（→ §4.5）：ハードゲート【PASS必須】／WARN系AI癖【非ブロック＝出荷可】／
        フル本物化(選択肢C)【任意STEP】。「PASSまで無限ループ」を避ける＝Cは出したい回だけ明示実行。
      ・出力＝「番号: 本文」形式（>>Nアンカー保持・欠番の不規則化は creative-thread-gen 側の責務）
STEP3 解説対象＆挿入位置の選定 → 解説者パート生成  →  /youtube-script-parts
      ・難用語/固有名から解説対象をN個抽出 →【提案→確認】
      ・各解説の挿入位置も【提案→確認】＝「対象用語クラスタが一段落した直後（次の話題へ移る前）」
        ※「用語初出の直後」ではない（初出は壁の途中になりがち）。平将門例：大蔵省は8初出だが解説は15直後
      ・冒頭/前置き(1本)/締め＋解説×N を生成
STEP4 アセンブル【本スキルの心臓部】→ /scripts/assemble_csv.py
      ・構成順に結合し、話者・大/下・返信先・解説挿入・句読点改行を機械付与（§6）
      ・スレは read-only（アセンブルは番号を振り直さない＝§7）
      ・出力＝CSV(utf-8-sig) ＋ 副産物 _台本.md(人間可読) ＋ ~/Desktop へCSV複製（**xlsxは廃止＝オーナー決定2026-06-20**・会話ビューアも作らない）
STEP4.5 敵対的"本物らしさ"監査【必須・機械ゲートの穴を埋める】→ §4.6 の5レンズLLM監査
      ・must_fix（講義臭=ナレVO化／二重主語/文末表現の文頭/会話流れ）を捕捉→修正→再qa →【提案→確認】
STEP5 規約3段階チェック → /youtube-script-checker（8層＋LLM文脈＋ファクト）
      ・近代政治色（GHQ/米軍/進駐軍 等）検出→【歴史一般化方針】で修正（→ §4.7）
      ・現代の実在個人はカテゴリ7で🔴→匿名化
STEP6 読み仮名dic → /ymm4-dic-generator（毎回書き換え・漏れ/誤読ゼロ）
STEP7 (任意) 発音チェック /ymm4-pronunciation-checker、タイトル/サムネ/概要欄 /youtube-script-parts、投稿予約
```

## 4.5 品質バー（3段の合格ライン）
- **ハードゲート＝PASS必須**：字数／禁止語(死/殺す/アホ/バカ/クソ/カス 等)／SLUR・ヘイト／
  規約🔴／時代タグFAIL／ASCII"。**1つでも引っかかれば出荷不可**（自動承認しない）。
- **WARN系AI癖＝非ブロック（出荷可）**：SCAF密度／感情温度／壁化／9f密度／単一語尾過集中／
  ナレ接着頭／長文独り語り 等。デフォルトはWARNのまま出荷可（毎回フル平準化はしない）。
- **フル本物化（選択肢C）＝任意STEP**：長文を淡々・ぶつ切りに総リライトしWARNも全消し。
  看板回など"本物2ch質感"を最優先する時だけ実行。重いので毎回はやらない。
- 「本物らしさの天井」（評価締めの同一ビート反復／時系列一本道／死にレス少なめ）は**意図的に追わない**。

## 4.6 敵対的"本物らしさ"監査（STEP4.5の中身）※機械ゲートが拾えない content を捕捉
ultracode 5レンズ（並列LLM監査）。平将門で must_fix を出した実績ある工程＝**これが無いと品質が落ちる**。
- レンズ＝①なんJ語感 ②アンカー整合 ③講義臭 ④AI臭残留 ⑤規約
- 必ず捕捉する content パターン（機械ゲート化しにくい＝今回の must_fix）：
  - **講義臭＝ナレVO化**：元号＋西暦キャプション＋終止形現在で長文を始める（掲示板の実況ナレ化）
    → 掲示板口調へ（例「940年、天慶3年やな。…敗れてまう」）。
  - **二重主語の文法よれ**（「将門は…討伐軍が苦戦しとった」）→ 述語一本化。
  - **文末専用表現の文頭使用**（「知らんけど」を文頭に置く 等）→ 文末へ。
  - **返信アンカーが会話の流れと噛み合うか**（§6.6 の後方ジャンプ＝①66）。
- 偽陽性対策：サイズ交絡・辞書バイアス・スクレイプ崩れは棄却（平将門の8軸監査で実証済）。
- must_fix を出したら該当レスを修正→**再qaでPASS維持を確認**→ユーザーに差分提示→確認。

## 4.7 近代政治色の歴史一般化方針（STEP5の修正指針）
youtube-script-checker が近代政治の固有名（GHQ／米軍／進駐軍／特定政党 等）を検出したら、
**実在の近代政治色を排し歴史的記述へ一般化**して修正する：
- 例：GHQ/進駐軍 → 「戦後の区画整理」「重機」「国家事業」「お役所」。
- 検出だけで終わらせず、この一般化までを修正方針として明記（平将門で実施済）。

## 4.8 最終受入チェック（出荷前の決定的検証）※今回手作業だった検証をスキル化
組み立て後、**機械的に整合性を検証**してから「完成」と言う（平将門で手打ちした検証の常設化）：
- 解説が**指定本数ぶん発火**したか（既定=スレ①2本/スレ②2本＝計4）。after_seqが実在し正しい位置に入ったか。
- **大に `>>` が付いていないか**（下大→大の帰結）／大の直後に下・>>が無いか。
- **返信マップ突合**：`confirmed reply_map件数 − 大/直後で抑制された件数 = 出力CSVの>>件数`。
  差が出たら不整合。**抑制で消えた返信リンク一覧を可視化**して確認ゲートに出す（黙って消さない＝§6.5⚠）。
- 返信先 `>>M` が全て**自分より前(seq)**を指すか（未来/自己参照ゼロ）。
- スレタイが各スレ先頭・男性Aか。
- CSV：列数・utf-8-sig・句読点改行・行数。
- 規約：削除対象語（現代実在個人の実名 等）が本文に残っていないか（例 太田=0）。
- 不一致が1つでもあれば**完成と言わず**該当を修正→再検証。

## 5. 入出力
**入力**：テーマ（人名/事件）、前半後半の振り分け方針、解説対象の希望（任意）、保存先フォルダ
**出力（保存先 = `01_プロジェクト/YouTube/創作スレ下書き/YYYY-MM-DD_テーマ`）**：
- `_台本.csv`（YMM4取り込み・utf-8-sig・句読点改行）＝**唯一の出荷フォーマット**
- `_台本.md`（人間可読・構成見出し付き）
- `~/Desktop` への複製（CSV/dic）
- ※**xlsxは廃止**（オーナー決定2026-06-20）。目視は show_widget の会話ビューアを単発依頼で出す
- `ymm4_user.dic`（STEP6）
- ※会話ビューア（show_widget）は**作らない**（オーナー判断＝不要）。レビューしたい時だけ単発依頼で出す

## 6. CSV組み立て規約【正本＝refs/組み立て規約.md にも複製】
### 6.1 列構成
`登場人物, セリフ, 備考, 番号`（番号は「連番」または「連番>>返信先連番」）
### 6.2 構成順（備考列のセクション）
冒頭 → スレ① →〔解説:用語A〕→〔解説:用語B〕→ 前置き → スレ② →〔解説:用語C〕→〔解説:用語D〕→ 締め
※解説は**対象用語クラスタが一段落した直後（次の話題へ移る前）**にスレの合間へ挿入。
　**「用語初出の直後」ではない**（初出は壁の途中になりがち）→ 位置は §4 STEP3 で提案→確認。
### 6.3 話者
- スレタイ（各スレ1レス目）＝**男性A**
- 以後 **男性A→女性B→男性C→女性D→男性E** をレス順にローテ
- 解説者パートは全部「解説者」
### 6.4 大（長文ラベル）
- `>>`/`↑`除去後の本文が **88字以上 → 大**（字数は**句読点改行を付与する前の生本文**で数える＝§7.2の順序）。
- 88は平将門のレス長分布での暫定値。**境界±5字（83〜92字）は「大候補→提案→確認」のグレーゾーン**に回す。
  learning-logには「88」でなく「長文＝大の判定ルール」を記録（§7.5）。
### 6.5 下・返信先（番号列 `N>>M`）※返信グラフはLLMが判断(§7.1)、scriptはラベル化のみ
- 返信（本文`>>`始まり or スレタイ反応 or **LLMが返信と判断したレス**）→ **下** ＋ 番号 `N>>M`（M=返信先のスレ内**連番seq**）。
- **スレタイへの反応＝`>>1`**。バースト範囲＝**概ねseq2〜5**。個別レスを引き始めた/話題が一段具体化したら終了（範囲はLLM判断→確認）。
- **下大→大（大優先）**：長文かつ返信でも「大」にし、下も>>も付けない。
- **大の直後のレスは下も>>も付けない**（返信でも素の番号）。
- ⚠️ この2つの抑制で**LLMが承認した返信エントリの一部が出力CSVから消える**（平将門で約16%＝12件）。
  消えた返信リンクは§4.8受入チェックで**件数突合＆一覧可視化**（黙って消さない）。
### 6.6 返信先は「会話の流れ」準拠（最重要・平将門①66の教訓）※判定はLLM
- 返信先は**直近 or 同一話題のwall**へ。後方ジャンプ（独立トピッククラスタが完結した後に前へ戻る＝①66）は不自然。
- 妥当性判定は**§4.6敵対監査レンズ②でLLMが本文意味から**行う（regexのgap閾値は誤検出＝>>1反応/wall集中返信を誤爆し①66=gap1は取りこぼし）。
- **機械の自動補正（gap≥3で直近へ寄せる）は廃止**（§7のLLM判断降格と矛盾＋実証で破綻）。scriptは寄せない＝LLMが材料を見て決める。
### 6.7 句読点改行（TTS用）
- セリフは `。、！？` の直後で改行（既存改行は一旦解除してから付与）
### 6.8 文字コード・出荷物
- CSV=UTF-8(BOM付き/utf-8-sig)・LF。**出荷はCSVのみ**（xlsx廃止2026-06-20）。目視は会話ビューアを単発で。
### 6.9 句点(。)・読点(、)は【あえてゲートしない】※将来の事故防止メモ
- 本物2chは句点が極端に少ない＝AI臭の"穴"ではあるが、**うちのCSVはTTS用に句読点改行を使う**ため
  句点/読点を削ると改行ロジックが壊れる。よって**句点系の整然さは意図的にゲート対象外**にしている。
- 将来この"穴"を機械ゲートで塞ごうとしないこと（TTSフローと衝突する＝平将門8軸監査での確定判断）。

## 7. 役割分担：LLM判断（提案→確認）と script機械適用 ※ドライランで確定
**ドライラン知見(2026-06-20)**：返信マップのregex自動導出を thread2 で検証→手マップと**50%しか一致せず**
（完全一致20/40・先違い6・取りこぼし14・余計13、会話流れWARNも誤爆17）。
原因＝本物スレの`>>`は疎ら（qa-9g対策で壁にだけ付く）で、「誰が誰に返してるか」は本文を読まないと分からない。
→ **返信マップ・解説位置・解説対象は決定的に導出できない＝LLM判断タスク**と判明。役割を分ける：

### 7.1 LLM判断（スキルが読んで推論→提案→ユーザー確認→manifestへ）※regex化しない
- **返信マップ（会話の返信グラフ）**：スレを読み、誰が誰に返してるかを §6.5/6.6 のルールで解釈。
  スレの明示`>>N`は手がかりの一部に過ぎない。短い反応→直近wall／スレタイ反応→>>1／後方ジャンプ(§6.6)回避、
  を**LLMが判断**して決める（regexは50%で破綻＝実証済）。
- **解説の挿入位置**：対象用語クラスタの末尾（§6.2）を読んで判断。**初出直後ではない**。
- **解説対象の選定**：難用語/固有名トップN。
- いずれも **提案→確認ゲート必須**。確定値を §8 のmanifestに載せて 7.2 へ渡す。

### 7.2 script機械適用（assemble_csv.py）※ただし下/>>は返信マップに依存＝「手判定ゼロ」ではない
- **パイプライン順序（固定）**：①`>>`/`↑`除去 → ②大/下判定（**字数は改行付与前の生本文**）→
  ③返信マップ反映（番号列 `N>>M`）→ ④句読点改行 → ⑤CSV/MD出力（xlsx廃止）。
- ラベル付与の**優先順位**：`is_dai(88字+) ＞ prev_dai(大の直後) ＞ reply_map(下/>>)`。
  ＝大ルールが返信マップに優先するので**大×マップの相互作用で一部の>>が抑制される**（§6.5⚠・§4.8で突合）。
- 話者ローテは純index（ROT[i%5]）で決定的＝manifestに載せない。
- **スレ本文テキストは不改変（read-only）**。ただし番号列の`>>`付与（本文に無い`>>1`含む）は
  **LLMの返信マップに従いscriptが新規付与**＝「本文read-only」と「>>付与」は別物。
- スレ内部のraw番号（欠番あり）→seq連番の変換は**assemble_csv.py一箇所**がread-only入力時に機械生成。
  **reply_mapも解説位置もseq空間で扱う**（rawはmanifestに出さない）。
  ※平将門の「post-30一括振り直しで本文>>が壊れた」のは**スレ側を改変したから**＝二度とやらない。欠番不規則化はcreative-thread-gen側の責務。

## 7.5 自動化グラデーション（確認ゲート → 学習 → 段階自動化）※設計の軸
オーナー方針＝**「初期は各STEPで確認、ナレッジ/学習が溜まったらフル自動化も視野」**。
- **初期＝全STEPで確認ゲートON**。各STEP完了時に結果を提示し、ユーザー承認を取ってから次へ。
- 各STEPの判断を `learning-log.md` に記録。ただし**"値"でなく"判断ルール/修正パターン"を記録**する
  （毎回ネタが違うので具体値は汎化しない）。例：「解説対象＝難読固有名トップN」「①66型の後方ジャンプは直近へ」。
- **成熟度トラッカー**：同種の判断がK回連続（暫定K=3〜5）で「そのまま承認」されたSTEPは、
  スキルが「このSTEP、自動化していい？」と**提案**する（**提案専用＝勝手に自動化しない**）。
- ユーザー承認で**そのSTEPだけ自動モードにflip**（他STEPは確認のまま個別運用）。
- 自動化の進む順（判断が安定しやすい順）：STEP4アセンブル＞STEP6 dic＞STEP3解説対象選定＞…。
- creative-thread-genの学習ループ（収集→学習→**承認**→検証付き適用）と同一哲学＝
  **金/出荷に関わる所は実装者≠確認者の検証ゲートを残す**（rules.md準拠）。
- 完全自動運用に移っても、STEP5規約チェックの🔴とqa_checkのFAILは**常にハードゲート**（自動承認しない）。

## 8. 同梱物（skill配下）
- `scripts/assemble_csv.py` … threads(2)＋parts → CSV/MD（gen_csv.pyの汎用版）
  - **入力契約＝マニフェスト(JSON)**（ベタ書き廃止・全データはseq空間）：
    `{出力base, 冒頭パス, 前置きパス, 締めパス,
      スレ①:{threadパス, reply_map:{seq:seq,…(>>1も明示)}, 解説:[{after_seq, partパス, ラベル}…]},
      スレ②:{同上}}`
  - **reply_map＝LLMが§6.5/6.6で判断した返信グラフ**（gen_csv.pyのREPLY1/2と同形・seqキーseq値）。
    scriptはこれを受けて§6.5でラベル化するだけ＝**返信先をscriptが自動導出するのではない**（§7.1）。
  - **after_seq＝再採番後の連番**（raw原スレ番号は載せない）。指定seqが実在しなければ**即エラー**
    （旧gen_csvはキー不在を黙ってスキップ＝解説が無言で消える穴があった）。
  - 話者ローテ・大下ラベル・句読点改行のみscriptが規約(§6)で機械付与。
  - **§4.8受入チェックを内蔵**し、NG（未来/自己参照・大に>>・スレタイ不正・解説本数不一致）なら**exit 2**（"完成"と言わせない）。
  - 副産物＝`_台本.md`(人間可読)＋`~/Desktop`へCSV複製。**xlsxは出さない**（to_xlsx.py廃止）。
- `refs/組み立て規約.md` … 6章の正本（人間レビュー用）＋manifest正本スキーマ（sectionsリスト形）
- `refs/サンプル/` … 平将門の最終CSVを参照見本として同梱
- `learning-log.md` … 各STEPの判断ログ（9章の自動化グラデーション用）
- ※会話ビューアのテンプレは同梱しない（不要との確定）

## 9. エッジケース・失敗モード
- 解説対象がスレに出てこない/0本/多すぎ → ユーザーに代替提案（用語追加 or 本数調整 or スキップ）。
- **解説の指定 after_seq が実在しない → 即エラー**（サイレントスキップ禁止）。§4.8で本数発火を機械検証。
- qa/ハードゲートがPASSしない → creative-thread-genに差し戻し。**同一FAILが再生成K回(暫定2〜3)で直らなければ打ち切り→ユーザーへエスカレ**（無限ループ防止）。
- 壁化/WARN系 → スレ側の問題＝生成スキルへ（アセンブルでは直さない）。WARNは非ブロック出荷可（§4.5・暫定運用）。
- 会話流れ（①66型）の疑い → §4.6監査でLLMが提示→ユーザー確認（機械では寄せない）。
- **再実行/差分再生成(idempotency)**：中間生成物（スレmd・manifest）を保存し再利用。出力は上書き。
- **本文を改変する広域修正（頭リライト/フル本物化）の後**：字数が変わる→**大/下フラグ再判定→大⇄非大が反転したレスの返信マップをLLMへ再判断→差分再qa**を1セットで回す（平将門は再生成を3回実施）。
- 確認ゲートでユーザーが大量修正 → manifestに反映して再アセンブル（差分再生成）。

## 10. 既存youtube-pipelineへの波及（✅Phase3完了・2026-06-21）
- `assemble_csv.py` を共有エンジン化し、youtube-pipeline STEP2-4/STEP4 もこれを呼ぶよう差し替え。
- これで実スレ版／創作版の**両方が同一の組み立て規約**を使う＝規約の二重メンテ解消。
- ⚠️**設計の前提訂正**：当初「両者の差は生成ソースだけ・下流は同一」としたが、実装監査で**規約自体が食い違っていた**ことが判明＝(a)「大」の意味が youtube-pipeline=イッチ(OP1人) vs creative=88字+長文壁(複数) (b)返信が yt=「下」マーカーのみ vs creative=N>>M グラフ (c)出力 xlsx vs CSV。「下流同一」は誤り。
- **オーナー裁定A＝完全統一（creative規約に寄せる）2026-06-21**：youtube-pipeline の STEP2-4/4 を assemble_csv.py に差し替え、実スレ動画も creative規約（大=長文壁・スレタイ無印・N>>M・CSV出力）へ統一。手の大/下振り＋openpyxl変換は廃止。実スレは scrape→「番号:本文」化→manifest→engine。**実証済**：実スレ形式(欠番+明示>>)が同一エンジンで§4.8全PASS／既存2本(平将門差分0・崇徳全PASS)も回帰無傷。
- 既知エッジ：打線OP（長い列挙の1レス目）は creative規約では seq1=スレタイ=男性A無印で「大」が付かない（旧yt規約ではイッチ大）。打線スレは当チャンネルで稀のため当面許容。強調が要れば将来 seq1への override を検討。

## 11. 論点の確定（2026-06-20 オーナー回答）
1. **スコープ＝フルオーケストレーター（STEP1〜7）**。初期は**各STEPで確認ゲート**、
   ナレッジ/学習が溜まれば段階自動化（→ 7.5章）。✅確定
2. **解説対象の選定＝提案→確認**（後者）。同じく学習で自動化を視野。✅確定
3. **youtube-pipelineの扱い＝〔ベスト案〕「組み立てエンジンの一本化」**。
   - 本スキルが `assemble_csv.py`＋`refs/組み立て規約.md` の**正本**を持つ。
   - 第2フェーズで youtube-pipeline(実スレ版) もこのエンジンを参照/採用し、STEP2-4/4を差し替え。
   - 根拠＝両者の差は**生成ソース（scrape vs 創作生成）だけ**で、下流（組み立て→検品→dic→出荷）は同一。
     下流を一本化すれば規約の二重メンテが消える。当面は別スキル併存でOK、規約だけ共有。✅確定
4. **会話ビューア＝作らない**（不要）。✅確定
5. **配置＝〔ベスト案〕既存スキル同期トポロジを踏襲**。
   - 正本＝`~/.claude/skills/creative-thread-pipeline/`（編集は必ずここ）。
   - ポータブル複製＝`00_システム/20_Agent_Portable/codex-skills-ready/creative-thread-pipeline/`。
   - 根拠＝全スキルと同一配置＝既存の同期/移行の自動化にそのまま乗る（memory: skill-sync-topology）。✅確定

## 12. 次アクション（実装フェーズ）※フェーズ分割で出す
- **Phase 1（コア）**：オーケストレーション＋`assemble_csv.py`＋**全STEP確認ゲート**＋**STEP4.5敵対監査**。
  ＝§4の①②③⑤・(a)(b)・G1〜G6を反映した最小実用形。
- **Phase 2**：`learning-log.md`＋自動化グラデーション（§7.5・ルール記録）。
- **Phase 3**：youtube-pipeline へ `assemble_csv.py` をエンジン共有（§10）。
- 実装メモ：
  - skill-creator で `~/.claude/skills/creative-thread-pipeline/` を生成。
  - SKILL.md＝本設計書の3〜10章を手順化（各STEPに確認ゲート＋learning-log）。
  - `scripts/assemble_csv.py`＝gen_csv.py汎用化（**返信マップはLLM判断→manifest供給**・**スレread-only**・seq空間・大下/句読点のみ機械付与・§4.8内蔵・CSV出力のみ）。
  - `refs/組み立て規約.md`＋`refs/サンプル/`（平将門CSV＋manifest）同梱。**to_xlsx.pyは作らない**（CSV出力に確定）。
  - ポータブル複製を作成し同期確認。

## 16. Phase1 実装完了ログ（2026-06-20）
- `~/.claude/skills/creative-thread-pipeline/`（SKILL.md＋scripts/assemble_csv.py＋refs/組み立て規約.md＋refs/サンプル/＋learning-log.md）生成。ポータブル複製も完全一致で同期。
- **出力＝CSVのみに確定**（オーナー指示）。xlsx/to_xlsx.py は廃止。目視は show_widget の会話ビューア（単発）。
- assemble_csv.py に **§4.8受入チェック内蔵＋NGでexit 2**、section path を manifest相対で解決（CWD非依存）、`_台本.md`＋Desktop複製を追加。
- **回帰テスト合格**：平将門 manifest から本番CSV(174行)を**差分0で再現**。§4.8全PASS（解説4本発火・大に>>なし・未来参照なし・抑制12件可視化）。
- manifest正本スキーマ＝**sectionsリスト形**（§8ネスト案から確定）。`refs/組み立て規約.md`に記載。

## 17. 初回実走＋スキル検証ログ（2026-06-21）
**初回end-to-end実走（崇徳天皇・275行/8592字）でSTEP1-6完走**。STEP2がハードゲート（字数/禁止語）で2回弾いて差し戻し→PASS、STEP4.5でナレVO化10箇所修正、STEP5ファクト0誤り、STEP6 dic 319。設計どおりの差し戻し・再収束ループが機能。
**8エージェント敵対監査（4観点×監査→裏取り）でスキル自体を検証**。grade=自己整合性B/出力品質B-/堅牢性C/工程健全性C。real確定→対応：
- ✅**コード修正済**：①同一after_seq重複で解説が無言消失（dict後勝ち＋§4.8期待値が自己無効化＝設計§9「無言消失禁止」の2つ目の穴）→**即エラー化＋inserts_expectedを元配列長で計数**。②出荷物のASCII"が組立段で無検査→**§4.8にASCII"ゲート追加**。③スレタイ受入が`label.startswith("スレ")`依存で列挙系ラベルだと誤NG(exit2)→**スレタイ話者記録のlabel非依存方式**に変更。回帰（平将門差分0・崇徳全PASS）＋負テスト（重複→exit1）で確認。
- ✅**文書修正済**：§7.2のxlsx残骸→CSV/MD。**§4.8は構造整合のみ＝会話の意味的正しさは検査しない（突合は恒等式）＝STEP4.5の責務**、を組み立て規約＋code注記に明記（最重要の過信ポイント是正）。STEP3に**ナレ文量上限（解説200字級・ナレ総量はスレ本文の25%目安）**を追加（崇徳ナレ36%肥大の是正）。
- ⏳**要検討（設計トレードオフ・未着手）**：⑤88字グレーゾーン(83-92)の提案ゲートがscript未実装（ハードカット一本）。⑥最終CSVの安価率検品が無い（生スレで測りLLMがreply_mapで盛る＝出荷物の安価率が本物の約2倍）。⑦reply_map手起こし約100件/回の負荷（明示>>骨格の自動投入で半自動化余地）。⑧Phase2全自動化は確認ゲート約11箇所で現実性低い。
- 訂正：「長文ファーストで初稿一発率↑」はn=1仮説（効果未測定）。次走で初稿字数の一発/再走を記録して検証する。
- 残＝Phase2（learning-log自動化グラデーション）／Phase3（youtube-pipelineへエンジン共有）／崇徳の仕上げ（発音・サムネ・投稿）は任意。

## 18. Phase3 完了ログ（2026-06-21・youtube-pipelineエンジン共有）
- オーナー裁定A（完全統一）で実施。**assemble_csv.py＋組み立て規約.md が実スレ版・創作版の共通正本**に。
- youtube-pipeline SKILL.md 改修：①大/下サフィックス規約を creative と統一（大=88字+壁・スレタイ無印・N>>M・優先順位明記＝正本は creative-thread-pipeline/refs/組み立て規約.md）②STEP2-1＝scrape後は手で大/下を振らず「番号:本文」スレファイル化（ASCII"→「」）③STEP2-4＝手結合mdを廃し manifest→assemble_csv.py（seed_reply_map.pyでreply_map骨格・after_seqクラスタ末尾）④STEP4＝openpyxl変換を廃しエンジン出力CSVを確認/任意アップロード⑤STEP3は組立済_台本.md/CSVを検査⑥最終成果物表をCSVへ。
- youtube-pipeline独自の上流(thread_scraper.py)・下流(規約3段階checker/dic/タイトル概要欄)は不変。**STEP4.5敵対監査は実スレには非適用**（AI生成でないため＝source固有）。
- 検証：実スレ形式demoが共有エンジンで§4.8全PASS・既存2本回帰無傷。正本/ポータブル同期済。

## 19. Phase2 完了ログ（2026-06-21・自動化グラデーション）
- §7.5を実装＝`scripts/maturity.py`＋`automation-state.json`。STEP別に mode(manual/auto)/streak/history を機械管理。
- **鉄則を実装で担保**：①提案専用（flipはユーザー承認後の手動コマンドのみ）②STEP5は hard_gate＝auto化を**コードで拒否**（🔴/FAIL/qa NEEDS-FIXは永久manual）③auto中に modified が来たら**自動でmanual降格**④autoは確認スキップのみ＝機械ゲートは必ず実行。
- 運用：開始時 `maturity.py status`／各STEP後 `record <STEP> approved|modified`／K(=3)回連続approvedで「💡自動化提案」→ユーザーOKで `flip <STEP> auto`。自動化順＝STEP4＞STEP6＞STEP3。
- 崇徳実績を投入（STEP3/4/6=連続1・STEP2/4.5=リセット・STEP5=ロック）。**flip提案はまだ無し**（K=3に未到達＝あと2回の安定承認が必要＝設計どおり慎重）。負テスト（STEP5 auto化拒否）合格。
- これで Phase1(実装)→実走検証→敵対監査ハードニング→Phase3(エンジン共有)→Phase2(自動化グラデーション) が全着地。残＝実運用で各STEPの streak を積む／崇徳・将門の仕上げ。

## 13. レビュー反映ログ（2026-06-20）
平将門の全工程と突き合わせ、設計の穴を反映：
- ①解説位置=初出直後は誤り→クラスタ末尾＋確認（§4 STEP3/§6.2/§7-3）
- ②返信マップはLLM判断（regex自動導出は実走で破綻＝§14・§7-1）
- ③フル本物化は任意STEP＋WARN非ブロック（§4.5）
- ④アセンブルはスレread-only（番号振り直さない・欠番は生成側）（§7-2）
- ⑤assemble_csv.py の入力契約=マニフェスト定義（§8）
- ⑥learning-logは"ルール/修正パターン"を記録（§7.5）
- G1 敵対的本物らしさ監査(5レンズ)をSTEP4.5として追加（§4.6）★最重要
- G2/G3/G4 講義臭(ナレVO化)・二重主語・文末表現の文頭 をG1監査で捕捉（§4.6）
- G5 近代政治色の歴史一般化方針（§4.7/STEP5）
- G6 句点はあえてゲートしない理由を記録（§6.9）

## 14. ドライラン結果（2026-06-20・実装前テスト）
最大リスクの仮定「返信マップ自動導出」を既存thread2(アンカー無傷)で検証＝**設計の誤りを発見**：
- regex自動導出 vs 手作業の正解マップ：**完全一致 20/40件(50%)**・先違い6・取りこぼし14・余計13・WARN誤爆17。
- 結論＝**返信マップはregexで導出不可。LLM判断タスク**（本文を読んで返信グラフを解釈）。
  → §7を「LLM判断(提案→確認) ＋ script機械適用」に再構成（最重要の設計修正）。
- 副次：解説位置・解説対象も同じくLLM判断（§7.1）。creative-thread-genの初稿WARNは品質バー(§4.5)で吸収済＝設計への脅威なし。
- **教訓＝机上レビューでは出ず、実走で初めて出た穴。実装前テストの価値が出た。**
- 未テスト残：fresh ネタでの end-to-end（生成→LLM返信判断→アセンブル→監査）。Phase 1実装後の初回実走で確認する。

## 15. 設計穴監査の反映（2026-06-20・6レンズ×13エージェント・13件）
机上では出なかった穴を、実データ(gen_csv/最終CSV/スレ)突き合わせで発見→反映：
**HIGH（実装ブロッカー）**
- H1 manifestに`reply_map`が無く§8が「scriptが返信先自動付与」と自己矛盾 → §8に`reply_map`(seq:seq)追加・自己矛盾解消・§12/13の旧語修正。
- H2 解説挿入キーがraw/seq未定義（gen_csvはraw{16,30}、設計例はseq「15直後」）＋キー不在で解説が無言で消える → §8で`after_seq`(連番)に固定・即エラー化（§9）・本数発火を§4.8で検証。
- H3 大ルールがLLM承認済み返信を約16%(12件)黙って破棄 → §7.2を「手判定ゼロ」撤回・優先順位明記・§4.8で件数突合＆消えたリンク可視化（§6.5⚠）。
**MED**
- M4 返信マップの番号空間(raw/seq)と変換責任未定義 → seq一本化・raw→seq変換はscript一箇所（§7.2/§8）。
- M5 §6.6の機械自動補正(gap≥3)が§7のLLM判断降格と矛盾＋誤検出 → 機械補正廃止・LLM判断へ一本化（§6.6/§9）。
- M6 88字=大が裸の定数 → 境界±5字はグレーゾーン→確認・learning-logはルール記録（§6.4）。
- M8 スレタイ反応バーストの終端規則なし＋read-onlyと>>1新規付与の字義矛盾 → バースト境界(概ねseq2〜5)明記・「本文read-only」と「>>付与」を分離（§6.5/§7.2）。
- M9 解説ドラフトの**採用版選択＋アンカー割当**が確認ゲートに無い（p_maeoki1/2・maeoki.md・p_kaisetsu1/2の複数候補から人が選別） → **STEP3に「複数ドラフトから採用版を選び、どの解説をどのafter_seqに紐付けるか」を提案→確認サブステップとして追加**。manifestには採用版パスとafter_seqの対応のみ載せる。
- M10 平将門で確立した**「型」の既定値が未記載** → **既定値を明記**：解説=各スレ2本(計4)・各2〜4セル／冒頭5〜6・前置き3〜4・締め5〜6セル／>>1バースト概ねseq2〜5。すべて「既定＋LLMが増減提案→確認」。N選定ルール(その語を知らないと議論が追えない固有名トップN)はlearning-logへ。
- M11 再実行/差分再生成/大量修正の再収束ループ未設計 → §9に追加。
**LOW**
- L7 工程順（特に大判定の字数が句読点改行の前後）未明記 → §7.2に固定順序を明記。
- L12 ハードゲートが直らない時のループ上限/エスカレ未定義 → §9にK回打ち切り。
- L13 WARN非ブロック出荷は実走実績ゼロ（平将門の唯一の出荷はC到達後）→ **§4.5に「WARN非ブロック出荷は実績ゼロの暫定運用」と注記**し、Phase1初回実走で事後評価（§14未テスト残に追記）。
**棄却された偽穴**：>>除去は既に対処済／話者ローテをmanifestに載せない判断は正しい（純indexで決定的）／ローテmod5末尾衝突は飾りで実害薄。

---
> 関連: [[claude-code-codex-obsidian-operation]] / 既存スキル youtube-pipeline・creative-thread-gen・ymm4-dic-generator
