# 要確認

祐馬さんの判断が必要なもの。

削除、外部投稿、会計確定、自動化停止、新規ジョブ作成、外部アカウント設定変更はここに置いて確認する。

## 確認待ち

### ✅クローズ済み: redrive滞留 `1521301688667865158` の裁き

2026-07-03 Codex読み取り調査。`~/.claude/redrive/queue/1521301688667865158.json` は処理・移動・削除せず、判断待ち。

- 依頼要約: 制限状態の確認（本文全文はここに載せない）
- 対象: `chat_id=1486946641389817899` / `message_id=1521301688667865158` / `ts=2026-06-29T23:49:46.997Z`
- 現状: 同じIDが `queue/` と `dlq/` の両方に存在。`dlq` 側は `attempts=3`
- ログ証拠: 2026-06-30 09:06 / 09:11 / 09:16 に自動配達が発火したが、Claude週次上限中で3回失敗してDLQ行き。10:06 / 10:11 / 10:16 に再度同じIDが失敗して再DLQ。11:06 に `confirm-mode.flag` が作られ、以後は確認モード
- 分類: **B: 発火したが失敗**。補助原因として、DLQ済みIDを `redrive_drain.py` が重複除外せず再queue化している疑いあり。6/29 watchdog改修による回帰は疑い止まりで、現証拠だけでは断定しない
- 処理済み痕跡: `REDRIVE_DONE 1521301688667865158` は未検出。Obsidian内の既存ヒットは過去監査レポートのみ

**✅裁き済み（2026-07-03 祐馬さん）: 選択2＋選択3で確定**
- 依頼 `1521301688667865158` は古い（6/29深夜の制限状態照会）ため**今から処理しない**
- 残骸整理（queue/dlq退避・削除なし）＋confirm-mode解除＋3欠陥修正（drainのDLQ照合漏れ／上限中のattempts消費／確認モード降格の無通知）を **ai-development-flowの修理ブリーフ1本**に統合。Fable司令塔・Codex実行・Fable検証
- 状態: **完全クローズ（2026-07-04 Phase 5煙テストPASS）**

クローズ結果:
- Phase 3+4で `redrive_drain.py` / `limit_redrive.py` / `watchtower_local.py` を修理済み（各 `.bak-20260704-redrive-repair` あり）
- 古いqueue/dlq残骸は削除せず `~/.claude/redrive/skipped/20260704-stale-1521301688667865158/` へ退避済み
- `confirm-mode.flag` は `confirm-mode.flag.resolved-20260704-stale` へリネーム済み
- Phase 5で `redrivetest002` をサブチャンネル `1486953436212625458` に対して煙テスト。自然tickで `done/redrivetest002.json` へ移動、ledgerに `REDRIVE_DONE: redrivetest002`、サブチャンネルに `煙テストOK🌊` の返信着信を確認
- launchd操作・リスナー再起動・削除は未実施

### 2026-07-05 vault-snapshot 売上証憑入り確認

2026-07-03のFable司令塔調査で、`restore-drill` の last exit 1 は故障ではなく「修正前の6/28 tarに売上証憑が入っていない」ことをカナリア検査が正しく検知した結果と整理済み。

- 6/28 tar: `売上証憑` なし。2026-07-02 04:30の定期演習はこれを検査して `FAIL (1件)` / exit 1
- 7/2 tar: `売上証憑/出前館/2026/20260601-0615_出前館_配達実績_9437.pdf` 入り
- 最新state: `/Users/kojinn/.claude/scripts/restore-drill-state.json` は `result = PASS`、`last_run = 2026-07-02T17:53:09`、`files_verified = 4271`
- 運用上の正判定: Watchtowerが参照する `restore-drill-state.json`
- launchctlの `last exit code = 1` は次回の月次restore-drill実行まで残る参考値

次に見ること:
- 2026-07-05 4:30の週次 `vault-snapshot` が、launchd経由でも `売上証憑` 入りtarを作れるか
- 2026-07-05朝以降に `vault-snapshot-2026-07-05.tar.gz` の中へ `売上証憑` が1件以上入っていれば、この件はクローズ
- 入っていなければ、`vault-snapshot` のlaunchd/TCC/agent-runフォールバック調査を `ai-development-flow` で別件化する

今回やっていないこと:
- launchd操作
- restore-drill手動実行
- スクリプト修正
- バックアップ実体の変更
- Drive証憑の操作

### 会計正本整理（→NOW.md 参照）

会計関連の残務（kicho記帳停止の後始末／freee-uncleared-monitor 再開判断 など）は、進捗・判断事項とも **NOW.md の「会計正本整理（正本セクション）」に集約済み**。重複管理を避けるため、この件はここでは追わない。

- 正本: `06_エージェント運用/00_司令塔/NOW.md` の「会計正本整理（正本セクション・ここに集約）」
- 状況（2026-06-28時点）: 停止整合・後始末はすべて完了。残るは `freee-uncleared-monitor` を再開するか否かの判断のみ（任意・急ぎでない）。

> ※ 上記に同伴していた〔会計スコープ外〕の redrive滞留メモ（`~/.claude/redrive/queue/1520246027058282677.json` の「タスク見せて」）も NOW.md 末尾「残る判断事項」へ移設済み。
