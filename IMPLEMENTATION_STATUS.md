---
updated: 2026-06-24
---

# IMPLEMENTATION_STATUS

バックアップ・正本トポロジの実装状況。何が完了済みで、何が未完了かを一覧化する。

## 憲法（1行）

正本は常にローカル（テキスト/コード=Mac内蔵、巨大メディア=外付けSSD）。Drive と GitHub はコピー専用で直接書かない。AIは必ずローカルの正本を編集する。例外は2つだけ: 税の証憑(経費精算) と Googleネイティブ書類(.gsheet/.gdoc) は当面 Drive が正本。

---

## 1. ✅ 完了済み（現状で達成できていること）

- [x] **4正本の git + GitHub 日次 push** — 正本4つ（`~/2nd-Brain-master` 知識20M / `~/Projects/youtube` 作業・台本5.8M / `~/agent-skills` スキル2.2M / `~/agent-adapters` AI入口244K）が全てローカルgit。10分毎 auto-commit → GitHub private（org=corocorobusiness-oss）へ日次push＝コードの遠隔コピーが成立。
- [x] **Drive の知識ミラー** — `~/2nd-Brain` を Drive `マイドライブ/2nd-Brain` へ15分毎の片方向ミラー（閲覧専用）。Drive側・他端末で直接編集しない運用。
- [x] **週次 snapshot → SSD** — 日曜4:30に週次スナップショット（tar → `~/claude-backups`）→ 外付けSSD（マウント時 ssd-backup）。`claude-backups` はSSD上の週次tarミラー。
- [x] **税証憑の実3コピー＋復元テストPASS** — 経費精算（16M）は Drive＋Mac内tar＋SSD内tar の実3コピー。復元テスト 2026-06-16 にPASS（729件 sha256照合）。
- [x] **大容量メディアを外付けSSDへ退避済み** — 動画メディア正本（`02_完成` 395G・完成動画227本＋プロジェクト ほか / YouTube 13G編集素材・YMM4本体 / Coin Quest / SSD：アプリケーション10G）を外付けSSD（931GB・空き447GB）に集約。内蔵256GB（空き約84GB）に巨大メディアを置かない方針を実装。
- [x] **agent-run 継ぎ目の存在** — `~/agent-adapters/bin/agent-run` が Claude↔Codex 切替の唯一の継ぎ目として存在する。

---

## 2. 🔧 未完了・要対応（低リスク・今週）

- [ ] **売上証憑が穴** — 売上証憑（出前館PDF・32K、正本=Drive）は現状どのバックアップにも無い＝唯一の本当の穴。`vault-snapshot.sh` の対象リストに `売上証憑/` を1行追加すれば git/SSD に乗り解消（合意済み・着手OK）。
- [ ] **外付けSSDのゴミ 8.4G 未整理** — `_backups`（8.4G・YMM4移行スナップショット重複疑い）＋空殻フォルダを精査 → `_削除待ち` へ隔離 → 中身確認後に削除。鉄則「消す前に別媒体にコピー済みを確認」。
- [ ] **各正本に README 無し** — 各正本フォルダ直下にREADME 1行（「ここは何の正本か/正本はここだけ/コピーはどこ」）を未配置。4正本分を作成する。
- [ ] **CLAUDE.md に正本見分けルール未記載** — CLAUDE.md に1行追記（「実体がローカルにあり .git があるフォルダだけが正本。Drive配下と .gsheet ポインタは正本でない＝税証憑・gsheet原生のみ例外」）が未対応。

---

## 3. 🟡 オーナー判断待ち（推奨つき）

- [ ] **動画395Gのオフサイト** — 要判断 / 推奨: 2台目SSD（約1万円）へ週次ミラーで媒体2を即充足＋四半期に物理搬送でオフサイト1。未実施。
- [ ] **Codex 非依存化（週次 snapshot の Claude 依存切り）** — 要判断 / 推奨: 後回し。当面 Claude 常駐前提。Codex移行を決めた時に launchd直＋rsync化＋スクリプトを agent-run 経由へ。
- [ ] **gsheet 542点 どれを守るか** — 要判断 / 推奨: 業務クリティカルな数点だけ月次/四半期 Google Takeout で .xlsx/.docx 化 → `~/2nd-Brain-master/_証憑取込/gsheet-export/` に落とし git化。全件は過剰。対象選定は祐馬。
- [ ] **電帳法の税証憑 正本=Drive 固定の見直し** — 要判断 / 推奨: 当面 Drive 正本のまま例外運用（規程改定＋税務確認は急がない。既に実3コピーで復元PASS済み）。

---

## 4. ⛔ 既知の制約／残存リスク

- **週次 snapshot の Claude 依存** — 週次スナップショットは Drive を `claude -p` 経由で読む（macOS TCC制約）＝Claude常駐に依存。
- **agent-run 未配線で Codex 切替不可** — `agent-run` は継ぎ目として存在するが、どのスクリプトもまだ使っておらず全部 `claude` を直叩き、codex側も未配線＝今は Codex 切替は実質不可。約30本の launchd/cron スクリプトが claude を直接呼ぶ。
- **動画 SSD 単独** — 動画メディア（`02_完成` 395G ほか）の正本は外付けSSD。現状SSD単独＝穴。
- **gsheet Google 依存** — クラウド表計算（.gsheet/.gdoc 542点）の正本=Google。ローカルは223Bポインタのみ＝実体コピー無し。

---

## 優先順位まとめ（今週 → 中期 → 大物）

### 今週（低リスク・着手OK）
1. 売上証憑を `vault-snapshot.sh` の対象に1行追加 → 唯一の本当の穴を消す。
2. 外付けSSDの `_backups` 8.4G＋空殻フォルダを精査 → `_削除待ち` へ隔離 → 中身確認後に削除（消す前に別媒体のコピーを確認）。
3. 各正本フォルダに README 1行を配置＋CLAUDE.md に正本見分けルールを1行追記。

### 中期（オーナー判断・小〜中）
4. gsheet の業務クリティカル数点を選定（祐馬）→ Takeout で .xlsx/.docx 化して git化。
5. 電帳法の税証憑 正本=Drive を当面据え置きで確定（規程改定は急がない）。
6. Codex 非依存化は後回し（Codex移行を決めた時に着手）。

### 大物（媒体追加が必要）
7. 動画395Gのオフサイト化（推奨: 2台目SSD週次ミラー＋四半期に物理搬送）。

---

## 安全原則（手順書に必ず明記）

- 消す前に別媒体のコピーを確認する。
- Drive に直接書かない・kill しない（Drive killall/再起動は禁止。TCCキャッシュが原因、リスナー再起動でのみ回復）。
- symlink を実体と間違えて編集・削除しない。
- エラーや中断は隠さず正直に報告する。
- お金や証憑に関わる破壊的操作は実行前に祐馬へ確認する。
