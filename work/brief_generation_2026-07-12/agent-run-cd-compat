#!/usr/bin/env bash
set -euo pipefail

workdir="$PWD"
args=()
while [ "$#" -gt 0 ]; do
  case "$1" in
    -C|--cd)
      workdir="$2"
      shift 2
      ;;
    *)
      args+=("$1")
      shift
      ;;
  esac
done

cd "$workdir"
exec /Users/kojinn/agent-adapters/bin/agent-run "${args[@]}"
