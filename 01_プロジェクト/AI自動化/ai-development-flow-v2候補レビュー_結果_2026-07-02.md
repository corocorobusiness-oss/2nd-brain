# ai-development-flow V2 候補レビュー結果 2026-07-02

## 対象

- `ai-development-flow-v2_SKILL候補_2026-07-02.md`
- `ai-development-flow-v2_openai.yaml候補_2026-07-02.md`
- `AI開発依頼テンプレ_完全版.md`
- `AI開発フロー_標準テンプレ.md`
- `ai-development-flow-v2移行設計_2026-07-02.md`

## 独立レビュー判定

3人の独立レビューで、P0はなし。

ただし、P1が出たため、候補初稿のままではV1退避・V2反映へ進めない。

```text
総合判定: 修正後採用可
P0: なし
P1: あり。修正済み。
現時点: V1稼働中 / V2候補修正済み / V1退避承認待ち
禁止: V2反映、試用開始、正式採用
```

## 修正したP1

1. 発動トリガーが広すぎる
   - 「自動化したい」「仕組み化したい」は、まず `automation-architect` 相談導線に残す。
   - 明示的に「開発依頼にして」「ai-development-flowに回して」と言われた時だけ、開発ブリーフ化する。

2. `handoff_mode: plan_only` の停止が弱い
   - plan-only は軽い案件でも実装しない。
   - 依頼文承認後は、案件分類、Goal、設計、実装計画までで必ず止める。

3. 危険操作承認フォーマットが正本と候補でズレている
   - `承認者 / 承認日時 / 二重実行防止` を正本テンプレ側にも追加した。
   - 1項目でも欠けたら危険操作承認として無効にした。

## 反映した補強

- 候補本文はMarkdownコードフェンス内だけを反映対象にする。
- 反映前に候補本文hashを記録し、反映後の実体hashと比較する。
- 正本ファイル欠落時は、相談・plan-only intake までに制限し、実装・反映・危険操作をしない。
- 依頼文承認後の返答に `危険トリガー候補: あり（具体名）/ なし / 未確認` を入れる。
- 実装開始承認の `危険操作は含まない` は YES のみ有効にした。

## まだやっていないこと

- `/Users/kojinn/agent-skills/ai-development-flow` の編集
- V1退避
- `manifest.sha256` 作成
- V2反映
- Codex runtime dry-run
- 5回試用
- 正式採用

## 次のゲート

次に進むなら、まず `V1退避` だけを別承認で行う。

その後、退避hash一致を人間が確認してから、別ゲートで `V2反映承認` を扱う。
