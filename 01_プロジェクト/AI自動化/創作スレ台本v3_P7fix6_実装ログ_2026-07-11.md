---
status: 実装完了_独立レビューPASS
phase: P7-fix6
approved_by: 池田祐馬
approval_date: 2026-07-11 JST
implemented_at: 2026-07-11 JST
---

# 創作スレ台本v3 P7-fix6 実装ログ

## 1. 目的

P7-fix5新テーマ3本で機械軸がPASSしてもSTEP7が止めた差を塞ぐ。単レスのdialogue role検査は維持したまま、会話全体について次の4軸を統合ledgerで監査する。

1. historical progression
2. semantic reply anchor
3. short conclusion / editing / lecture cluster
4. 前後半の同役割モチーフ再走

候補抽出は高再現・低精度とし、人/LLMが全候補を判定する。本文、manifest、CSVの自動編集は行わない。未判定、hash不一致、偽アンカー、確定違反はfail-closeする。

## 2. 承認

- 実装開始承認: 2026-07-11 JST、池田祐馬
- 危険操作承認: 2026-07-11 JST、池田祐馬、対象8ファイルの完全形式
- 対象: agent-skills 7ファイルと本ログのみ
- v1/v2/v3既存入口の置換・停止: なし
- `qa_check.py`、既存dialogue-role rules、採点表、盲検プロンプト、review-contract、brief-contract、eval既存記録の変更: なし

## 3. 実装前状態

- agent-skills: `848bd54963c1af1a890df23071547f5e01e2134c`
- Vault: `e1628249fa3234aab77e303ebbb04e1c5c022ad4`
- 対象pathの既存差分: 0
- 新規予定ファイル5件: すべて未存在
- 同等のP7-fix6契約、監査スクリプト、rules、test、fixture: 未存在

## 4. 実装差分

### agent-skills

- `creative-thread-gen/SKILL.md`: v3/P7-fix6 historical progression契約を追加
- `creative-thread-pipeline/SKILL.md`: 候補生成、人/LLMレビュー、hash付き再検証、STEP7件数整合手順を追加
- `creative-thread-pipeline/refs/組み立て規約.md`: 4監査軸とfail-close規約を追加
- `creative-thread-pipeline/scripts/audit_conversation_v3.py`: v3明示専用の統合監査を新規追加
- `creative-thread-pipeline/refs/v3-conversation-audit-rules.tsv`: 汎用候補抽出ルールを新規追加
- `creative-thread-pipeline/tests/test_audit_conversation_v3.py`: 単体・回帰・fail-closeテストを新規追加
- `creative-thread-pipeline/tests/fixtures/v3_conversation_audit_gold.json`: 既知FAIL3型と回帰2型のgold fixtureを新規追加

### 差分量

- 7ファイル、追加1510行、削除0行
- 既存文書3ファイルは追記のみ
- 新規コード・rules・tests・fixtureはP7-fix6を明示実行した時だけ参照される

## 5. 統合ledger契約

### 候補生成

- 必須入力: brief、front/back、manifest、assembled CSV、front/back role ledger
- 任意入力: front/back motif ledger
- 全入力とrulesのSHA-256を保存
- 候補だけを出し、確定修正や自動編集はしない

### 人/LLMレビュー後の再検証

- 各半3〜5件のrequired fact anchor
- required anchorごとに1件以上のfact advance
- 史実進行なし最大12レス
- speculative-only比率30%以下
- semantic replyはmanifestとCSVで同じ可視anchor
- independent投稿へのanchorはfake anchor
- short clusterとcross-thread motifの全候補に判定と根拠
- `review`残存、根拠なし許容、hash drift、role ledger未解決は停止

## 6. 検証

### 6.1 構文・単体・既存回帰

- `py_compile`: PASS
- 新規 `test_audit_conversation_v3.py`: 14 tests PASS
- 既存 `test_dialogue_role_gate.py`: 13 tests PASS
- 合計: 27 tests PASS
- `git diff --check`: PASS
- gold fixture JSON: PASS
- rules TSV: 12行、必須列・profile v3・正規表現を確認

### 6.2 fail-close

単体テストで次を確認した。

- 未判定候補: NEEDS_FIX
- source hash drift: NEEDS_FIX
- rules hash drift: NEEDS_FIX
- role ledger source/verified hash不一致: NEEDS_FIX
- role ledger `needs_fix` / `review_required` / `unresolved`: NEEDS_FIX
- machine candidate削除・改変: 入力から再生成した候補集合との不一致でNEEDS_FIX
- independent判定なのにanchorあり: fake anchorでNEEDS_FIX
- semantic replyのmanifest/CSV target欠落または不一致: NEEDS_FIX
- semantic replyのtargetが不存在または未来/自己参照: NEEDS_FIX
- `--profile v2`: exit 2、ledger未作成
- 候補生成前後でthread/manifest/CSV hash不変

### 6.3 P7-fix5既知FAIL3本

eval既存記録は変更せず、出力ledgerを `/tmp` に保存した。

| 対象 | 検出結果 | integrity error |
|---|---|---:|
| 平治の乱 | progression proxy front最大29/back38レス、raw speculative 55.6%/58.3%、semantic未anchor 10/8、cross-thread motif候補31 | 0 |
| 田沼意次 | progression proxy最大65/57レス、semantic未anchor 13/16、short cluster 1/1 | 0 |
| 薩英戦争 | progression proxy最大113/150レス、raw speculative 36.8%/40.0%、semantic未anchor 5/7、short cluster 1/1 | 0 |

3本すべてでSTEP7既知失敗に対応する監査軸を候補化した。候補だけで確定FAILにせず、人/LLM判定待ちの `REVIEW_REQUIRED` とした。

### 6.4 既知回帰2本

- 元寇B3fix1 front/back: P7-fix5 role gate PASS、統合監査候補生成 exit 0 / `REVIEW_REQUIRED` / integrity error 0
- 2026-07-09 応天門PASS front/back: P7-fix5 role gate PASS、統合監査候補生成 exit 0 / `REVIEW_REQUIRED` / integrity error 0
- 旧runのmanifest reply_mapとCSV可視anchorには大レス等の抑制差がある。これを入力破損として自動FAILにせず候補へ出し、semantic replyと人/LLMが確定した場合だけ可視anchor欠落として停止する。
- gold control 2型は、全候補をレビュー済みにした状態でverify PASS。

### 6.5 v1/v2/v3既定挙動

- 既存コード `qa_check.py` は変更していない。
- 新スクリプトは独立CLIで、`--profile v3` と明示呼び出しが必要。
- `--profile v2` はexit 2でledgerを書かない。
- v1/v2/v3既存入口からの自動呼び出しは追加していない。

## 7. auto-commit境界

- agent-skillsの対象7ファイルはauto-commit `a952e61541597e7c9fc1516e4f385a13934e58f2` に取り込まれた。
- fail-close境界修正3ファイルはauto-commit `3923a91e6ea174b000a69ee07bf8f6446e030fa9` に取り込まれた。
- Vaultログはauto-commit `74fdcc2ed0ea227259f04b8dc581a0cabe8f4675` に取り込まれた後、独立レビュー結果を追記した。
- 同commitに本件外の次の3ファイルが並行作業として混載された。本件の実装・レビュー対象から除外する。
  - `creative-thread-gen/data/thread_ledger.json`
  - `creative-thread-gen/data/thread_ledger.json.bak`
  - `creative-thread-pipeline/automation-state.json`
- 本件の累積差分は、実装前commitから承認済み7pathだけを指定して確認する。
- `3923a91` にも本件外の `creative-thread-pipeline/automation-state.json` が混載された。本件レビュー対象から除外する。

## 8. 独立レビュー

- 独立エージェント: Plato（read-only、実装者とは別）
- 初回判定: 差し戻し
  - manifest/CSV/reviewed targetが一致しても、不存在番号・未来/自己参照なら通る穴
  - role ledgerの必須schema・side・source path・rules hash・posts/summary型を検査しない穴
  - 薩英fixtureの期待軸にhistorical progressionが未反映
- 対応:
  - target実在性と `target < source` をverifyで強制
  - role ledger必須keys、profile、thread、source path、schema/rules、型、hash、3種の未解決状態をfail-close
  - 不在/未来anchor、role構造/side、review_required/unresolvedの境界テストを追加
  - 薩英fixture期待軸をprogression/semantic/clusterへ同期
- 軽量再レビュー: PASS
- 未解消重大指摘: なし
- fixture独立承認: 承認可。fixtureへ `independently_reviewed` を記録
- P7-fix6実装完了扱い: 可
- 軽微な検証限界: role ledgerの`summary`がobject以外の場合、構造化NEEDS_FIXの記録後に例外停止する可能性がある。fail-openや出荷継続にはならない。

## 9. 戻し方

- 既存3ファイルは対象pathだけagent-skills実装前commit `848bd54963c1af1a890df23071547f5e01e2134c` へ戻す。
- 本ログはVault実装前commit `e1628249fa3234aab77e303ebbb04e1c5c022ad4` を参照して履歴復元する。
- 新規script/rules/test/fixture/logの削除は別途承認を取る。未承認時は既存入口から呼ばず無効のまま残す。
- 本件外3ファイルは戻さない。

## 10. 未実施

- P7-fix6新テーマ検証run。実装差分の独立レビューPASS後に別ゲートで行う。
- v3昇格。新テーマrunが合格しても提案までで、自動昇格しない。
