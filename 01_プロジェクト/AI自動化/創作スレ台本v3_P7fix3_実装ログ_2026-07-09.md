# 創作スレ台本v3 P7-fix3 実装ログ

- 日付: 2026-07-09 JST
- 実装者: Codex
- 承認者: 池田祐馬
- 対象: P7-fix3のみ
- 実装前commit:
  - agent-skills: `09300d56a089f328cf534b4ed925cfa60cd1b02f`
  - vault: `d3010b4a2fa7af4012e517fc320b86717bff07ae`

## 1. 承認記録

### 実装開始承認

P7-fix3のみ。v3専用で、制作方針メタ発言の禁止、モチーフ反復台帳、原因整理・記録読み・構成方針レス禁止を追加する。C3/C5/B3の同型FAILを防ぐため、追加ゲートより生成契約の上限制御を優先する。v1/v2/v3既存運用は置換・停止しない。

### 危険操作承認

以下7件の危険操作承認を受領済み。

1. `creative-thread-gen/SKILL.md` へP7-fix3 v3制作方針メタ発言禁止契約を追加
2. `creative-thread-gen/qa_check.py` へv3専用meta/motif/cause補助検出を追加
3. `creative-thread-gen/data/v2_conclusion_cluster.tsv` をP7-fix3用に強化
4. `creative-thread-gen/data/v2_lecture_rewrite_rules.tsv` をP7-fix3用に強化
5. `creative-thread-pipeline/SKILL.md` へP7-fix3 motif_ledger手順を追加
6. `creative-thread-pipeline/refs/組み立て規約.md` へP7-fix3 motif_ledger規約を追加
7. 本ログを新規作成

## 2. 実装差分

### creative-thread-gen

- `SKILL.md`
  - `v3/P7-fix3 制作方針メタ発言・モチーフ反復禁止契約` を追加。
  - 住人レスが「授業になる」「小論になる」「まとめに寄る」「ここでは止める」「戻ろう」などの制作/編集方針を喋ることを禁止。
  - 原因整理、記録読み、構成方針レスを禁止し、具体物・疑問・誤読・茶々へ崩す方針を明記。
- `qa_check.py`
  - v3専用の強制修正カテゴリとして以下を `ABSTRACT_FRAME_CATS` に追加。
    - `v3_meta_production_control`
    - `v3_motif_reset_loop`
    - `v3_cause_chain_list`
    - `v3_record_reading_voice`
    - `v3_editorial_balance_comment`
  - 既存のv1/v2既定profile処理は変更なし。
- `data/v2_conclusion_cluster.tsv`
  - v3専用行を5件追加。
  - 制作方針メタ発言、モチーフ話題リセット、原因整理束、記録読み講義、構成方針コメントをcluster検出対象化。
- `data/v2_lecture_rewrite_rules.tsv`
  - v3専用行を5件追加。
  - 上記5カテゴリを単発レスでも検出できるよう補助。

### creative-thread-pipeline

- `SKILL.md`
  - `v3/P7-fix3 motif_ledger / meta-speech ledger` を追加。
  - `motif_id` / `surface` / `thread` / `seq` / `role` / `action` の台帳を定義。
  - `editorial_control` / `cause_chain` / `record_reading` は原則 `rewrite` または `drop`。
- `refs/組み立て規約.md`
  - `v3/P7-fix3 motif_ledger規約` を追加。
  - 同一モチーフ・同一役割の前半/後半/終盤再出現をC3/C5/B3候補として扱う。
  - 制作方針メタ発言、原因整理、記録読み、構成方針コメントを禁止規約に追加。

## 3. 検証結果

### 3.1 py_compile

実行:

```bash
python3 -c 'import py_compile; py_compile.compile("/Users/kojinn/agent-skills/creative-thread-gen/qa_check.py", cfile="/tmp/qa_check_p7fix3.pyc", doraise=True); print("py_compile PASS")'
```

結果: `py_compile PASS`

### 3.2 v1/v2既定profileへの影響

- `profile_rules()` は未変更。
- 追加TSV行はすべて `rule_id` が `v3_` で始まるv3専用行。
- TSV確認:
  - `v3_rule_rows_total 31`
  - `v2_visible_v3_rows 0`
- `ABSTRACT_FRAME_CATS` の追加カテゴリは、既存ロジック上 `profile == 'v3'` のときだけ強制修正判定に使われる。

結論: v1/v2既定profileへの挙動変更は意図していない。既存v1/v2運用は置換・停止していない。

### 3.3 P7-fix2 大塩平八郎の乱 既知FAIL検出

対象:

- `/Users/kojinn/Projects/youtube/eval/p7fix2_trials/2026-07-08_大塩平八郎の乱_p7fix2_trial/threads/front_thread.md`
- `/Users/kojinn/Projects/youtube/eval/p7fix2_trials/2026-07-08_大塩平八郎の乱_p7fix2_trial/threads/back_thread.md`

コマンド:

```bash
python3 /Users/kojinn/agent-skills/creative-thread-gen/qa_check.py <thread.md> --profile v3 --lecture-rewrite-gate --conclusion-cluster-gate --terminal-cap-gate --filler-repetition-gate
```

結果:

- front: `v2 conclusion cluster NEEDS_FIX5`
  - `v3_meta_production_control`
  - `v3_motif_reset_loop`
  - viewer_takeaway / v3_closing_proxy など
- back: `v2 lecture rewrite gate NEEDS_FIX1`、`v2 conclusion cluster NEEDS_FIX7`
  - `v3_editorial_balance_comment`
  - `v3_meta_production_control`
  - `v3_motif_reset_loop`
  - `v3_record_reading_voice`

結論: P7-fix2の既知FAIL型（制作方針メタ発言、米びつ/桶/路地系モチーフ再走、原因整理、記録読み）を検出できる。

### 3.4 P7v3 元寇B3fix1 PASS版 過剰NEEDS_FIX確認

対象:

- `/Users/kojinn/Projects/youtube/eval/p7_v3_trials/2026-07-08_元寇_p7v3_b3fix1/front_thread.md`
- `/Users/kojinn/Projects/youtube/eval/p7_v3_trials/2026-07-08_元寇_p7v3_b3fix1/back_thread.md`

同コマンドで確認。

結果:

- front: `NEEDS_FIX0`
  - lecture rewrite gate はWARN12のみ
  - conclusion cluster NEEDS_FIX0
  - terminal cap NEEDS_FIX0
- back: `NEEDS_FIX0`
  - lecture rewrite gate はWARN16のみ
  - conclusion cluster NEEDS_FIX0
  - terminal cap NEEDS_FIX0

結論: P7-fix3追加による過剰NEEDS_FIXは確認されなかった。

### 3.5 差分検査

- `git -C /Users/kojinn/agent-skills diff --check`: PASS
- agent-skills側の実diffは承認済み6ファイルのみ。
- vault側の実diffは本ログ1ファイルのみ。

## 4. 戻し方

agent-skills側は以下の実装前commitから対象pathだけ戻す。

```bash
git -C /Users/kojinn/agent-skills checkout 09300d56a089f328cf534b4ed925cfa60cd1b02f -- \
  creative-thread-gen/SKILL.md \
  creative-thread-gen/qa_check.py \
  creative-thread-gen/data/v2_conclusion_cluster.tsv \
  creative-thread-gen/data/v2_lecture_rewrite_rules.tsv \
  creative-thread-pipeline/SKILL.md \
  creative-thread-pipeline/refs/組み立て規約.md
```

本ログはvault側commit `d3010b4a2fa7af4012e517fc320b86717bff07ae` から戻す。削除が必要な場合は別途承認を取る。

## 5. 未実施事項

- P7-fix3実装差分の第三者レビューは未実施。
- P7-fix3新テーマ検証runは未実施。
- 新テーマrunでv3昇格はしない。昇格は池田祐馬本人の別承認が必要。

## 6. 検証限界

- 今回の機械検証は、既知FAIL（大塩平八郎）と既知PASS回帰（元寇B3fix1）の2系統に限定。
- motif_ledgerは手順/規約として追加したが、新テーマrunでの実台帳作成・人/LLM確認は未実施。
- qa_checkの追加カテゴリは高再現寄りの補助検出であり、最終判定はrubric盲検と第三者レビューで確認する。

## 7. 次ゲート

1. P7-fix3実装差分の第三者レビュー（read-only）。
2. レビューPASS後、P7-fix3新テーマ1本検証runの実装開始承認。
