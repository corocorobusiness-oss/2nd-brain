# 創作スレ台本v3 P7-fix 実装ログ（2026-07-08）

## 1. 承認記録

- 実装開始承認: 池田祐馬 / 2026-07-08 JST / P7-fixのみ
- 危険操作承認: 対象7ファイル分を同時承認済み
- 承認範囲: v3専用 sidecar claim選定の要旨一致検出、review候補の自動採用禁止、関連SKILL/規約/brief契約追記、実装ログ作成
- 禁止: v1/v2の置換・停止、既存案件フォルダ台本本体、eval/runs既存記録の書き換え、asset-pipeline、採点表、盲検プロンプト、review-contract、常駐ジョブ、外部送信

## 2. 実装差分

- `select_sidecar_claims_v2.py`
  - v3専用で `forbidden_paraphrases` / `same_density_tests` / `verdict_essence` を読み、同密度言い換えを `semantic_overlap_level` として検出。
  - v3のfull explanation selectedは `review_reasons=[]` / `reject_reasons=[]` / `lexical_match=false` / `semantic_overlap_level=none` のclean候補だけに制限。
  - `review_reasons` 付き候補、`name_only_hit`、`same_density_paraphrase` は自動selectedにしない。
- `check_claim_leak.py`
  - v3補助フィールドをclaim phrase/token/semantic検出に追加。
  - `thread_before_insert` / `other_thread` / `closing` の同密度言い換え候補を高再現ショートリストに出す。
- `creative-thread-pipeline/SKILL.md` / `refs/組み立て規約.md`
  - P7-fix v3選定手順・clean selected条件・停止条件を追記。
- `kaisetsu-v2-guide.md`
  - v3解説契約に、review候補をfull explanation化しない規約を追記。
- `brief-contract.md`
  - v3任意補助フィールド `forbidden_paraphrases` / `same_density_tests` / `verdict_essence` を追記。schema_versionは維持。

## 3. 検証結果

### 3.1 構文確認

- `select_sidecar_claims_v2.py` / `check_claim_leak.py` を `compile()` で構文確認: PASS

### 3.2 明暦の大火 既知FAIL候補

- 入力: `/Users/kojinn/Projects/youtube/eval/p7_v3_trials/2026-07-08_明暦の大火_p7v3_trial/brief.json`
- `select_sidecar_claims_v2.py --profile v3`: `insufficient_clean_candidates`
  - selected: 0 / required: 2-3
  - `s1 振袖火事`: `reject` / `semantic_overlap_level=high` / `same_density_paraphrase`
  - `s2 火除地と広小路`: `reject` / `semantic_overlap_level=high` / `same_density_paraphrase`
  - label/term単独hitのgloss候補は `review` 止まりで、full explanation selectedには入らない
- `check_claim_leak.py --profile v3 --manifest --csv --c1-precheck`: PASS実行
  - candidates: 44
  - semantic candidates: 32
  - needs_fix_if_same_density candidates: 14
  - C1 precheck: `k1.c1` / `k2.c1` とも `reject`（`lexical_match`, `multiple_thread_evidence`, `estimated_c1_over_40`）

### 3.3 元寇 P7/v3 B3 fix1 回帰

- 入力: `/Users/kojinn/Projects/youtube/eval/p7_v3_trials/2026-07-08_元寇_p7v3_b3fix1/brief.json`
- `select_sidecar_claims_v2.py --profile v3`: `insufficient_clean_candidates`
  - selected: 1 / required: 2-3
  - `蒙古襲来絵詞`: clean candidateとして残存
  - `合浦`: `same_density_paraphrase` でreject
  - `てつはう`: short annotation / manual review
- 解釈: P7-fixは安全側に倒れ、clean候補が2本未満なら停止する。元寇B3 fix1は既存PASS素材だが、P7-fix基準では再選定が必要。

### 3.4 P4 中国大返し PASS版 回帰

- 入力: `/Users/kojinn/Projects/youtube/eval/p4_trials/2026-07-05_中国大返し_p4_replymap/`
- 注意: P4版はv3 `reserved_claims.json` を持たないため、同一条件のsidecar選定回帰は不可。代替として `v2_guard_packet.json` + assembled CSV で `check_claim_leak.py --profile v3` を実行。
- 結果:
  - candidates: 43
  - semantic candidates: 10
  - needs_fix_if_same_density candidates: 0
- 解釈: v3 reserved形式ではないため参考値。ただし過剰NEEDS_FIX相当は増えていない。

### 3.5 差分範囲

- agent-skills本体差分（auto-commit `9c596e1`）:
  - `creative-thread-pipeline/SKILL.md`
  - `creative-thread-pipeline/refs/組み立て規約.md`
  - `creative-thread-pipeline/scripts/check_claim_leak.py`
  - `creative-thread-pipeline/scripts/select_sidecar_claims_v2.py`
  - `neta-forge/refs/brief-contract.md`
  - `youtube-script-parts/references/kaisetsu-v2-guide.md`
- 追加補正:
  - `check_claim_leak.py` のCSV候補にも `semantic_hits` を記録する小修正を実施（承認済み対象内）
- Vault新規ログ:
  - 本ファイルのみ

### 3.6 第三者レビュー差し戻し対応（2026-07-08）

第三者レビューで、`check_claim_leak.py` のsemantic補助が `--profile v3` でガードされていない重大指摘を受領。対応として、`semantic_rule_hits()` / `score_claim()` にprofileを渡し、semantic加点は `profile == "v3"` のときだけ発動するよう修正した。

再検証:

- `check_claim_leak.py` 構文確認: PASS
- 明暦の大火 `check_claim_leak.py --profile v2`:
  - candidates: 18
  - semantic candidates: 0
  - 旧v2相当の候補数に戻ることを確認
- 明暦の大火 `check_claim_leak.py --profile v3`:
  - candidates: 44
  - semantic candidates: 32
  - v3だけsemantic補助が発動することを確認
- 明暦の大火 `select_sidecar_claims_v2.py --profile v3`:
  - `insufficient_clean_candidates`
  - selected: 0 / required: 2-3
  - `s1 振袖火事` / `s2 火除地と広小路` は `same_density_paraphrase` でreject

保存済みeval成果物について:

- `/Users/kojinn/Projects/youtube/eval/p7_v3_trials/2026-07-08_明暦の大火_p7v3_trial/sidecar_claim_selection_machine.json`
- `/Users/kojinn/Projects/youtube/eval/p7_v3_trials/2026-07-08_明暦の大火_p7v3_trial/checks/claim_leak_v3.json`

上記はP7-fix実装承認の「eval/runs既存記録の書き換え禁止」「Projects/youtubeへの書き込み禁止」に従い、未更新のまま残している。このログの数値は、修正後スクリプトを `/tmp` 出力で再実行した検証結果であり、既存run成果物の上書きではない。

### 3.7 軽量再レビュー結果

- レビュー者: 独立サブエージェント（Euclid）
- 判定: PASS
- 未解消の重大指摘: なし
- 確認内容:
  - `check_claim_leak.py` のsemantic補助は `--profile v3` のときだけ発動する。
  - 明暦の大火で `--profile v2` は candidates 18 / semantic 0、`--profile v3` は candidates 44 / semantic 32。
  - `select_sidecar_claims_v2.py --profile v3` は `insufficient_clean_candidates` / selected 0 のまま。
  - 保存済みeval成果物を未更新にした理由と、検証値が `/tmp` 再実行由来であることはログ上で明記済み。
  - 戻し方は `9c596e1` / `49959f8` / 未コミットprofileガード修正 / `eb07b95` を含み矛盾なし。
- 次ゲート: P7-fix新テーマ検証runへ進んでよい。ただしeval書き込みと外部検索/ネットワーク利用が入るため、別途実装開始承認と危険操作承認が必要。

## 4. 戻し方

- agent-skills側:
  - 実装auto-commit: `9c596e1`
  - CSV `semantic_hits` 補正auto-commit: `49959f8`
  - 第三者レビュー指摘修正: 未コミット差分（`check_claim_leak.py` のprofileガード修正）
  - 実装前commit: `eb07b95`
  - 戻す場合は対象pathだけを `eb07b95` の状態へ戻す。v1/v2本線の置換・停止は行わない。
- Vault側:
  - 本ログは新規作成。削除が必要な場合は別途承認。

## 5. 未実施事項・検証限界

- 新テーマ1本のrubric盲検2体検証は未実施。P7-fix実装差分レビュー後に別途eval run承認を取って実行する。
- 同密度検出は高再現・低精度の補助。最終判定は盲検C1/C4/C6で見る。
- P4中国大返しはv3 `reserved_claims.json` を持たないため、P7-fixと完全同条件の回帰ではない。参考回帰として扱う。
- 既存P7/v3明暦runの保存済みJSON/MDは未更新。P7-fix後の新しい証跡をeval配下に保存するには、別途eval書き込み承認が必要。
