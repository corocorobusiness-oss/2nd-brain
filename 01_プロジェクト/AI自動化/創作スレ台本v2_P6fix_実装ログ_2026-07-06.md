# 創作スレ台本v2 P6-fix 実装ログ（2026-07-06）

## 承認

- 実装開始承認: 池田祐馬 / 2026-07-06 JST
- 対象範囲: P6-fixのみ。reserved_claims.json、full-artifact claim leak gate、natural-demand test、closing/opening/bridge v2契約、B3 abstract conclusion cluster計測層を追加する。v1は置換・停止しない。
- 危険操作承認: 対象ファイルごとに同時承認済み。
- 触ってよい場所: 承認文に列挙された agent-skills 10ファイル/新規ファイル、採点表正本2ファイル、Vault実装ログ1ファイルのみ。
- 触ってはいけない場所: v1の置換・停止、既存案件フォルダの台本本体、asset-pipeline、常駐ジョブ、外部送信が必要な処理、承認外の agent-skills/Vault/eval/Projects/youtube。

## 実装差分

### creative-thread-pipeline

- `creative-thread-pipeline/SKILL.md`
  - STEP1.5に natural-demand test を追加。
  - `gloss_targets[].demand_type` を確認し、`narrative_required` は原則として解説対象から外す運用にした。
  - P6-fix時のみ `reserved_claims.json` を eval/案件サンドボックスへ作る手順を追加。
  - STEP2で `qa_check.py --profile v2 --guard-packet ... --lecture-rewrite-gate --conclusion-cluster-gate` を使う手順を追加。
  - STEP3で closing/opening/bridge のP6-fix契約を追加。
  - STEP4.5で `check_claim_leak.py` による全文artifact漏れ候補検査を追加。
- `creative-thread-pipeline/refs/組み立て規約.md`
  - §6.11 P6-fix reserved_claims / full-artifact claim leak を追加。
  - `same_density=true` が closing / same_thread_later / other_thread に残る場合は NEEDS_FIX と明記。
  - C4 guardとの優先関係、allowed_prelude と forbidden_zones の優先関係、E軸記録を明記。
- `creative-thread-pipeline/scripts/check_claim_leak.py`（新規）
  - `reserved_claims.json` または旧 `v2_guard_packet.json` の `guards[]` を読み、全文artifactからclaim漏れ候補を列挙する補助スクリプト。
  - 自動修正・自動FAIL判定はしない。高再現・低精度のショートリストとして出す。

### youtube-script-parts

- `youtube-script-parts/SKILL.md`
  - v2/P6-fixの締めモードを追加。
  - `reserved_claims.json` の `verdict_essence` を negative constraints として扱う手順を追加。
- `youtube-script-parts/references/kaisetsu-v2-guide.md`
  - `reserved_claims` 出力規約を追加。
  - `demand_type` は `narrative_required` / `meta_background` / `hybrid` に統一。
  - `thread_should_not_say` は全文漏れ検査へ渡せる粒度で書くよう明記。
- `youtube-script-parts/references/closing-v2-guide.md`（新規）
  - 締めナレは情報の再説明ゼロ・新規claimゼロ・`verdict_essence` 言い直し禁止。
  - 役割は感情の着地、余韻、次回誘導のみ。
  - opening/bridgeも「話題名の再掲はOK、claim結論の再掲はNG」と明記。

### creative-thread-gen

- `creative-thread-gen/SKILL.md`
  - P6-fix v2 conclusion cluster gate の手順を追加。
- `creative-thread-gen/qa_check.py`
  - `--conclusion-cluster-gate` を追加。
  - `--profile v2` かつ明示指定時のみ `v2_conclusion_cluster.tsv` を読む。
  - 終盤ウィンドウ、近接レス連鎖、評価/総括/教訓化ヒットの束を cluster 単位でWARN計測する。
  - 既定profile v1では新層は動かない。
- `creative-thread-gen/data/v2_conclusion_cluster.tsv`（新規）
  - summary / evaluation / causality / source_or_later / lesson_like の初期ルールを追加。

### neta-forge

- `neta-forge/refs/brief-contract.md`
  - 契約版を v1.2 に更新（`schema_version` は `"1"` のまま）。
  - `gloss_targets[]` に `demand_type` / `explainable_layer` / `thread_may_say[]` / `thread_should_not_say[]` を追加。
  - natural-demand test の実施者・タイミング（M5初期付与→STEP1.5確定）を明記。

### 採点正本

- `台本採点表.md`
  - rubric_version を v1.4 に更新。
  - C1/C6で `verdict_essence` レベルの同義再説明を重く見る規約を追加。
  - C6の対象に opening/bridge/closing を追加。
  - B3に abstract conclusion cluster（終盤の評価・総括レスの束、分担ナレ化）を追加。
- `台本採点_盲検プロンプト.md`
  - prompt_version を v1.4 に更新。
  - 採点者用手順・JSON schemaを v1.4 と同期。

## 検証結果

### 実行済み

- `py_compile`
  - 初回はmacOS Pythonのキャッシュ書き込み先権限で失敗。
  - `PYTHONPYCACHEPREFIX=/tmp/codex-p6fix-pycache` へ逃がして再実行しPASS。
- CLI確認
  - `qa_check.py --help` で `--conclusion-cluster-gate` が表示されることを確認。
  - `check_claim_leak.py --help` が表示されることを確認。
- v1既定profile互換
  - `/tmp/p6fix_thread.md` を既定profileで実行し、`v2 conclusion cluster` が出ないことを確認。
  - `--profile v2` だけでも conclusion cluster は出ず、明示指定が必要であることを確認。
- conclusion cluster 最小動作
  - `/tmp/p6fix_thread.md` に終盤の評価・総括レス3連鎖を置き、`--profile v2 --conclusion-cluster-gate` で `clusters1 / hits3` を確認。
- claim leak 最小動作
  - `/tmp/p6fix_reserved.json` + `/tmp/p6fix_thread.md` を `closing` artifactとして実行し、候補1件を確認。
- 旧P6 dry-run近似回帰
  - 旧P6 3テーマの `v2_guard_packet.json` を `reserved` 代替として使い、assembled CSV + closing で候補抽出が動くことを確認。
  - 承久の乱: 4 claims / 51 candidates
  - 桶狭間の戦い: 4 claims / 97 candidates
  - 赤穂事件: 4 claims / 40 candidates
- 中国大返しP4 PASS版の近似回帰
  - 旧 `v2_guard_packet.json` を使い、4 claims / 34 candidates を確認。
  - これは高再現ショートリストであり、自動FAILではない。過剰検出の最終判定は次回のP6-fix再試作runで、人/LLMが `same_density` を付けて判断する。

### 未実施・次ゲート

- 新形式 `reserved_claims.json` を持つ実runはまだ無い。P6-fix再試作runで初めて本運用検証する。
- P6失敗3本の「既知漏れ検出」は旧 `v2_guard_packet.json` による近似まで。正式にはP6-fix再試作runで `reserved_claims.json` を保存して検証する。
- rubric v1.4 / 盲検プロンプトv1.4での盲検採点は未実施。
- v2昇格は未実施。dry-run合格後もAIは提案までで、昇格承認は池田祐馬本人が別途行う。

## 戻し方

対象pathのみをgitで実装前へ戻す。新規ファイル削除が必要な場合は別途承認を取る。
採点利用後の正本修正は履歴を書き換えず、次版で訂正する。

## 注意

- `neta-forge/SLATE_RUNBOOK.md` は本実装前から存在した別作業由来の差分であり、本P6-fixでは触っていない。
- Vault側にも本件外の既存未コミット/未追跡ファイルがあるため、本ログではP6-fix対象pathのみを証跡対象にする。
