# AI開発フロー 初回運用レビュー 2026-07-01

## 案件
AI開発フローを呼び出す薄いスキル `ai-development-flow` の作成。

## 分類
重要

理由:
- 今後の仕組み作り全体の入口になる
- 長く使う開発運用ルールに関わる
- 認証情報、外部送信、本番データ操作は扱わないため、危険案件ではない

## 実際に使ったエージェント編成
- 司令塔/実装AI: Codex
- QA/完成確認: ローカルファイル確認、YAMLパース確認、git status確認
- 外部AI/複数サブエージェント: 未使用

未使用理由:
- 今回はファイル追加中心で、秘密情報や外部送信を伴わない
- 重要案件だが、スキル本体は薄い入口であり、独立レビューは次回実使用後に回す方が実効性が高い

## 作ったもの
- `/Users/kojinn/agent-skills/ai-development-flow/SKILL.md`
- `/Users/kojinn/agent-skills/ai-development-flow/agents/openai.yaml`
- `00_システム/20_Agent_Portable/codex-skills-ready/ai-development-flow/SKILL.md`
- `00_システム/20_Agent_Portable/codex-skills-ready/ai-development-flow/agents/openai.yaml`

## 参照した正本
- `AI開発依頼テンプレ_完全版.md`
- `AI開発フロー_標準テンプレ.md`
- `AIエージェント編成ルール_天才会議_2026-07-01.md`

## 詰まった点
- `init_skill.py` 実行時、`short_description` が21文字判定で短すぎて `agents/openai.yaml` 生成前に停止した
- `generate_openai_yaml.py` と `quick_validate.py` は、この環境の Python に `yaml` モジュールがなく実行できなかった

## 重すぎた点
- スキル本体に標準テンプレ全文を入れる案は重すぎるため採用しなかった
- 正本はObsidianに置き、スキルは3ファイルを読む薄い入口にした

## 軽すぎた点
- 公式 `quick_validate.py` が実行できていないため、検証は代替確認に留まる
- 次回、PyYAMLを入れるか、skill-creator側の検証環境を整えるとよりよい

## 漏れた点
- 初回作成時点では、実際にこのスキルを別セッションから呼び出す forward-test は未実施だった
- その後、計画のみの forward-test を2件実施した

## よかった点
- スキル本体を薄くしたことで、Obsidian正本との二重管理を避けられた
- 危険案件トリガー、完成ゲート、外部AI除染ルールをスキル入口に入れられた
- `agents/openai.yaml` は手動作成後、RubyのYAMLパーサで確認できた
- forward-test で、案件分類、必要なエージェント編成、完成条件、非ゴール、リサーチ範囲、リスク、止まり方、実装計画、検証証拠が出力された

## 実行証拠
- `SKILL.md` frontmatter: Ruby YAMLパースでOK
- `agents/openai.yaml`: Ruby YAMLパースでOK
- `git -C /Users/kojinn/agent-skills status --short -- ai-development-flow`: `?? ai-development-flow/`
- forward-test 1: Obsidianデイリーノート追記の小仕組み。実装なしで初回計画のみ出力。分類は重要、危険化条件、dry-run、停止条件、検証証拠まで提示。
- forward-test 2: YouTube台本CSVからYMM4辞書登録候補を抽出するスクリプト。実装なしで初回計画のみ出力。分類は重要、既存辞書運用との衝突回避、非ゴール、停止条件、証拠まで提示。

## forward-test 判定
判定: PASS

理由:
- 2件とも、実装に入る前に計画を出した
- 2件とも、案件分類と分類理由を出した
- 2件とも、重要案件として必要なレビュー/完成判定の分離を提示した
- 2件とも、やらないこと、止まり方、検証証拠を出した
- 破壊的操作、ファイル編集、外部送信は行っていない

残リスク:
- 実際のスキル自動発火は次回以降のセッションで確認が必要
- 公式 `quick_validate.py` は PyYAML 不足で未実行
- 重要案件の本実装まで通した end-to-end 検証は未実施

## 次回から変えること
- `short_description` は25文字以上にする
- `quick_validate.py` が使えるように、PyYAMLの有無を先に確認する
- スキル作成が重要案件の場合、初回実使用後に別コンテキストで独立レビューする

## テンプレ改訂が必要か
現時点では必須改訂なし。

ただし、次回も `PyYAML` 不足で検証が止まる場合は、`AI開発依頼テンプレ_完全版.md` の「スキル作成時の検証」に代替YAML確認を追記する。

---

## 実案件 end-to-end 1: daily-note-append CLI

案件:
今日のデイリーノートがなければテンプレートから作成し、あればメモ欄へ追記するオンデマンドCLIを作る。

分類:
重要

危険案件ではない理由:
- 外部送信なし
- 認証情報なし
- 削除なし
- launchd / cron / 常駐化なし
- 本番日誌への検証書き込みなし

作ったもの:
- `00_システム/20_Agent_Portable/scripts/daily_note_append.py`
- `00_システム/20_Agent_Portable/specs/daily-note-append-cli.md`

台帳:
- `01_プロジェクト/AI自動化/導入済み.md` のオンデマンド欄へ追加

### 要件・テスト・証拠対応

| 要件ID | 要件 | 証拠 | 状態 |
|---|---|---|---|
| REQ-001 | 日誌がなければテンプレートから作成する | `/tmp/codex-daily-note-append-test` で `2026-07-05` 作成、終了コード0 | PASS |
| REQ-002 | 現行見出し `## 💡 メモ / アイデア` に追記する | `最終正常系A` / `最終正常系B` が連続追記された | PASS |
| REQ-003 | 旧形式 `### 💡 思いつきメモ / Inbox` に追記する | `2026-05-10.md` コピーで旧形式見出しへ追記、終了コード0 | PASS |
| REQ-004 | dry-runは書き込まない | `2026-07-06` dry-run後に `test ! -f` が終了コード0 | PASS |
| REQ-005 | 本番日誌dry-runは内容を変えない | `05_日誌/2026-07-01.md` のSHA256が前後で同一 `063157a1652ddaabb05a02d3a4dbc6bee2784cf723bab1129ef80ec1bd945d3a` | PASS |
| REQ-006 | 重複追記を避ける | 同一メモ再追記で `status: duplicate-noop` | PASS |
| REQ-007 | 複数行入力を扱う | `複数A` / `複数B` を箇条書き化。完全重複はno-op、一部重複は新規行のみ追加 | PASS |
| REQ-008 | 見出しなしはデフォルトで止まる | 見出しなしファイルで終了コード2 | PASS |
| REQ-009 | `--create-section` 付きなら見出しを作る | 見出しなしファイルに `## 💡 メモ / アイデア` を作成、終了コード0 | PASS |
| REQ-010 | 空文字は止まる | `--text ''` で終了コード2 | PASS |

### 独立レビュー

判定:
WARN

重大指摘:
- コード上の重大な仕様ズレ・即データ破壊リスクはなし
- 完成前に、git状態、dry-run非書き込み証拠、本番checksum不変、複数行入力の追加検証が必要

対応:
- dry-run後に対象ファイルが作られないことを `test ! -f` で確認
- 本番日誌dry-run前後のSHA256一致を確認
- 複数行入力の完全重複、一部重複、空行混じりを確認
- `git check-ignore` で追加ファイルがignore対象ではないことを確認
- `git status` で `05_日誌` に変更がないことを確認

未対応の重大指摘:
0

残WARN:
- 公式 `quick_validate.py` は PyYAML 不足で未実行。今回のCLI自体の完成には影響なし。

### 完成判定

判定:
完成

理由:
- 要件ごとの証拠が揃った
- 本番日誌への書き込みはしていない
- 独立レビューのWARNを追加検証で閉じた
- 外部送信、認証、常駐化、削除を含まない

次回改善:
- Python検証系でPyYAML不足が再発する場合は、代替YAML検証手順を標準テンプレへ追記する
- 実際にDiscord/スマホ受付からこのCLIを呼ぶ導線を作る場合は、外部入力・常駐化・自動実行ONを含むため危険案件として再分類する

---

## v1.2 スモークテスト: Goal固定 / 制限付き修正ループ

案件:
`/goal` と `/loop` 相当の運用を、コマンド依存ではなく `Goal固定` と `制限付き修正ループ` としてテンプレ・スキルに反映できているか確認する。

分類:
重要

Goal:
AI開発フローv1.2で、通常以上の案件はGoal固定を行い、失敗修正は最大3回で止まることが、標準テンプレ、依頼テンプレ、超短縮版、スキル入口のすべてから読み取れる状態にする。

やらないこと:
- 実在しない `/goal` `/loop` コマンドに依存させない
- 無限自律ループを許可しない
- 危険案件の人間承認ゲートを修正ループで迂回しない

確認結果:

| 確認項目 | 証拠 | 判定 |
|---|---|---|
| 標準テンプレにGoal固定がある | `AI開発フロー_標準テンプレ.md` の `0. Goal固定` と `2.1 Goal固定と制限付き修正ループ` | PASS |
| 標準テンプレに最大3回の制限付き修正ループがある | 同ファイルの `同じ失敗、同じテスト、同じ対象に対する修正ループは最大3回まで` | PASS |
| 依頼テンプレ本体にGoal固定と修正ループがある | `AI開発依頼テンプレ_完全版.md` の初回依頼文、AI側の進め方、完成報告テンプレ | PASS |
| 超短縮版にも反映されている | `AI開発依頼テンプレ_完全版.md` の `## 9. 超短縮版` | PASS |
| スキル入口にも短く反映されている | `/Users/kojinn/agent-skills/ai-development-flow/SKILL.md` の `bounded fix-test cycles` | PASS |
| スキルメタデータにも反映されている | `/Users/kojinn/agent-skills/ai-development-flow/agents/openai.yaml` の `bounded repair loops` | PASS |
| Vault内の再現用控えがある | `00_システム/20_Agent_Portable/codex-skills-ready/ai-development-flow/` | PASS |
| 正本スキルとVault内控えが一致している | `diff -u` で `SKILL.md` と `agents/openai.yaml` の差分なし | PASS |

実行証拠:
- `rg -n "Goal固定|制限付き修正ループ|最大3回|bounded repair loops|3 failed cycles|超短縮版" ...` で各ファイルの該当箇所を確認
- `git diff --check -- "01_プロジェクト/AI自動化/AI開発依頼テンプレ_完全版.md"` 終了コード0
- Ruby YAML確認で `skill ok`
- `diff -u /Users/kojinn/agent-skills/ai-development-flow/SKILL.md 00_システム/20_Agent_Portable/codex-skills-ready/ai-development-flow/SKILL.md` 終了コード0
- `diff -u /Users/kojinn/agent-skills/ai-development-flow/agents/openai.yaml 00_システム/20_Agent_Portable/codex-skills-ready/ai-development-flow/agents/openai.yaml` 終了コード0

残リスク:
- `quick_validate.py` は引き続き PyYAML 不足で未実行
- 今回の追記分とVault内控えは、次回のVault自動コミット対象

判定:
PASS

---

## 実案件 end-to-end 2: daily-note-append CLI v2

案件:
既存の `daily_note_append.py` を消さずに、同等以上の安全性を持つ `daily_note_append_v2.py` を横に作る。

分類:
重要

理由:
- 日誌に関わる繰り返し利用CLIである
- ただし外部送信、認証、削除、常駐化、launchd登録、本番日誌への検証書き込みは行わない

Goal:
既存CLIを壊さず、同等以上の安全性を持つ `daily_note_append_v2.py` を横に作り、`/tmp` 検証だけで完成候補にする。

やらないこと:
- 旧版削除
- 現行呼び出しの置換
- 本番日誌への実書き込み
- 外部送信
- launchd化 / 常駐化

作ったもの:
- `00_システム/20_Agent_Portable/scripts/daily_note_append_v2.py`
- `00_システム/20_Agent_Portable/scripts/test_daily_note_append_v2.py`
- `00_システム/20_Agent_Portable/specs/daily-note-append-cli-v2.md`

台帳:
- `01_プロジェクト/AI自動化/導入済み.md` に `daily-note-append CLI v2` を検証済み置換候補として追加

### 要件・テスト・証拠対応

| 要件ID | 要件 | 証拠 | 状態 |
|---|---|---|---|
| REQ-001 | 旧版を消さずv2を横に作る | `daily_note_append.py` はそのまま、`daily_note_append_v2.py` を追加 | PASS |
| REQ-002 | 新規日誌をテンプレートから作成する | `python3 -m unittest ...test_daily_note_append_v2.py` 13件PASS | PASS |
| REQ-003 | 現行見出しと旧見出しへ追記できる | unittestの現行見出し/旧見出しテストPASS | PASS |
| REQ-004 | 重複追記をno-opにする | unittestのduplicate/partial multilineテストPASS | PASS |
| REQ-005 | 見出しなしはデフォルトで止まる | unittestのmissing headingテストで終了コード2 | PASS |
| REQ-006 | `--create-section` 付きなら見出しを作る | unittestのcreate-sectionテストPASS | PASS |
| REQ-007 | dry-runは書き込まない | unittestのdry-runテストPASS、本番日誌dry-run前後SHA256一致 | PASS |
| REQ-008 | 空文字は止まる | unittestのempty textテストで終了コード2 | PASS |
| REQ-009 | 基本ケースで旧版と出力一致 | unittestのv1/v2 parityテストPASS | PASS |
| REQ-010 | 本番日誌コピーで旧版と出力一致し、本番実体を変えない | `/tmp` 影環境でv1/v2出力SHA256一致、本番日誌SHA256不変 | PASS |
| REQ-011 | 非ゼロ埋め日付を拒否する | `2026-7-1` は終了コード2、ファイル未作成 | PASS |
| REQ-012 | インデントされた `#` 行を旧版と同じ扱いにする | v1/v2 parityテストPASS | PASS |
| REQ-013 | 直接実行できる | `daily_note_append_v2.py --help` 終了コード0 | PASS |
| REQ-014 | テンプレート欠落時は新規日誌を作らない | unittestで終了コード2、ファイル未作成 | PASS |
| REQ-015 | `--allow-duplicate` の挙動を確認する | unittestで重複2件になることを確認 | PASS |

### 実行証拠

- `python3 -m unittest "00_システム/20_Agent_Portable/scripts/test_daily_note_append_v2.py"`: 13 tests OK
- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m py_compile ...`: 終了コード0
- `git diff --check -- ...daily_note_append_v2.py ...test_daily_note_append_v2.py ...daily-note-append-cli-v2.md`: 終了コード0
- `python3 ...daily_note_append_v2.py --date 2026-07-01 --dry-run "v2 dry-run checksum test"`: 終了コード0
- `05_日誌/2026-07-01.md` SHA256前後一致: `063157a1652ddaabb05a02d3a4dbc6bee2784cf723bab1129ef80ec1bd945d3a`
- `/tmp` 影環境で本番日誌コピーに `shadow compare memo` を追記: v1/v2とも終了コード0、出力SHA256 `fe55a39b9e12d9186f9d4de4002505d507d3e7d0216c4f7a5d160f15a7bb9d81` で一致
- 影環境比較後の本番日誌SHA256不変: `063157a1652ddaabb05a02d3a4dbc6bee2784cf723bab1129ef80ec1bd945d3a`
- `00_システム/20_Agent_Portable/scripts/daily_note_append_v2.py --help`: 終了コード0
- `ls -l`: v1/v2とも `-rwxr-xr-x`
- WARN対応後の影環境比較: `shadow compare memo after warn fix` でv1/v2出力SHA256 `91762fd20c95ef27aea5f180c1868a87719cc67c37bcb94891d7881db12d606f` 一致
- WARN対応後の非ゼロ埋め日付確認: `2026-7-1` は終了コード2、対象ファイル未作成
- WARN対応後も本番日誌SHA256不変: `063157a1652ddaabb05a02d3a4dbc6bee2784cf723bab1129ef80ec1bd945d3a`

### 制限付き修正ループ

ループ回数:
2

内容:
- `py_compile` がコード不具合ではなく、Pythonの `.pyc` 出力先がサンドボックス外 `~/Library/Caches` になって失敗
- `PYTHONPYCACHEPREFIX=/tmp/codex-pycache` を指定して再実行し、終了コード0
- 独立レビュー1回目で、非ゼロ埋め日付、実行権限、インデントされた `#` 行の見出し判定、不足テストをWARN指摘
- `--date` を `^\d{4}-\d{2}-\d{2}$` で厳密化
- 見出し境界を旧版と同じ `line.startswith("#")` に戻した
- v2に実行権限を付与
- 既存日誌dry-run、非ゼロ埋め日付、インデント#行、直接実行、テンプレート欠落、`--allow-duplicate` のテストを追加

追加改善:
- ローカルレビューで、テストが `/tmp` に一時ディレクトリを残す点を見つけたため、`self.addCleanup(shutil.rmtree, root, ignore_errors=True)` を追加

### 独立レビュー

実施状況:
1回目WARN / 対応済み / 再レビューPASS

理由:
- 1回目の独立レビューでWARN指摘あり
- 指摘対応後に再レビューを実施
- 再レビュー判定はPASS、重大指摘0、完成判定は完成可

レビュー再開用パケット:
- `01_プロジェクト/AI自動化/daily-note-append-v2_独立レビューパケット_2026-07-01.md`

未対応の重大指摘:
0

置換前に必要な条件:
- 正式置換は別作業として、人間確認後に呼び出し先変更またはv1統合を行う
- 置換作業後に同じ13件テストと、本番日誌コピーでのv1/v2相当比較を再実行する

### 完成判定

判定:
完成 / 現行置換前

理由:
- 実装、仕様、テスト、dry-run、本番日誌非変更確認は揃った
- 旧版は未削除、現行呼び出しも未置換
- 独立レビュー1回目WARNへの対応後、再レビューPASS
- v2自体は完成扱いにするが、現行CLIへの置換は別作業として扱う

---

## 実案件 end-to-end 3: daily-note-append CLI v2 正式差し替え

案件:
完成済みの `daily_note_append_v2.py` を、現行入口 `daily_note_append.py` に差し替える。

分類:
危険寄りの重要

理由:
- 既存CLIの置換に該当する
- ただし外部送信、認証、削除、常駐化、launchd登録、本番日誌への実書き込みは行わない
- ユーザーから「差し替えて」と明示指示あり

Goal:
旧版を戻せる形で保存したうえで、現行入口 `daily_note_append.py` をv2実装へ差し替え、テストと本番非変更確認を通す。

やらないこと:
- 本番日誌への実書き込み
- launchd / cron / 常駐化
- 外部送信
- 旧版削除

実施内容:
- 旧版 `daily_note_append.py` を `daily_note_append_v1_legacy.py` として保存
- `daily_note_append_v2.py` を `daily_note_append.py` にコピー
- テストを、旧版比較は `daily_note_append_v1_legacy.py`、現行入口確認は `daily_note_append.py` を見る形に更新

### 要件・テスト・証拠対応

| 要件ID | 要件 | 証拠 | 状態 |
|---|---|---|---|
| REQ-R001 | 旧版を残す | `daily_note_append_v1_legacy.py` を作成、SHA256 `573c5e187dc72b234e8c3aeb6c279107ec2abaab917d0bb5b1842a2cccc90376` | PASS |
| REQ-R002 | 現行入口をv2実装へ差し替える | `daily_note_append.py` と `daily_note_append_v2.py` のSHA256が同一 `992b921142aaa846c344b22b8eb3f2270f5fba4037bc4c5a29246b646a82da00` | PASS |
| REQ-R003 | 実行権限を維持する | `ls -l` で3ファイルとも `-rwxr-xr-x` | PASS |
| REQ-R004 | 置換後テストが通る | `python3 -m unittest ...test_daily_note_append_v2.py`: 14 tests OK | PASS |
| REQ-R005 | 構文チェックが通る | `PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m py_compile ...`: 終了コード0 | PASS |
| REQ-R006 | 現行入口dry-runは本番日誌を書き換えない | `daily_note_append.py --date 2026-07-01 --dry-run ...`: 終了コード0、本番SHA256不変 | PASS |
| REQ-R007 | 現行入口とv2の影環境出力が一致する | `/tmp` 影環境でSHA256 `a45d184845dc8ee617c64d1a7fd98e9167ab6d8616114a383f755727d122df45` 一致 | PASS |

### 実行証拠

- `python3 -m unittest "00_システム/20_Agent_Portable/scripts/test_daily_note_append_v2.py"`: 14 tests OK
- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m py_compile ...`: 終了コード0
- `python3 "00_システム/20_Agent_Portable/scripts/daily_note_append.py" --date 2026-07-01 --dry-run "post replace dry-run test"`: 終了コード0
- 置換後影環境比較: `current_v2_output_equal: true`
- 置換後影環境出力SHA256: `a45d184845dc8ee617c64d1a7fd98e9167ab6d8616114a383f755727d122df45`
- 本番日誌SHA256不変: `063157a1652ddaabb05a02d3a4dbc6bee2784cf723bab1129ef80ec1bd945d3a`

### 完成判定

判定:
完成

理由:
- 旧版を保存した
- 現行入口とv2の内容一致を確認した
- テスト、構文チェック、dry-run、本番非変更確認が通った
- 外部送信、認証、常駐化、削除、本番実書き込みを含まない

---

## フロー振り返り: daily-note-append v2 実案件後

判定:
フローは有効。

効いた点:
- 先にGoal固定したため、「v2を作る」と「現行入口を差し替える」を別工程に分けられた
- 旧版を残して横に作る方針により、置換前の比較が安全にできた
- `/tmp` 影環境比較により、本番日誌を書かずに実データに近い確認ができた
- 独立レビューで、非ゼロ埋め日付、実行権限、見出し境界という自分では見落としやすい点を拾えた
- 制限付き修正ループは2回で収まり、無限に粘らず完了できた
- 「完成候補」と「現行差し替え済み」を分けて記録できた

重すぎた点:
- 小さな単発CLIとしては、標準テンプレ全文のPhase 0〜14は重い
- ただし今回は「新フローの実戦検証」も兼ねていたため許容範囲

軽すぎた点:
- 既存置換案件の標準手順がテンプレ本文にはまだ薄い
- 置換後に「旧版退避、現行入口とv2のSHA一致、置換後テスト、影環境比較、戻し方」を確認する型は明文化した方がよい

漏れた点:
- 最初のv2仕様書に、置換後の状態を反映する文言更新まで含めていなかった
- 台帳の最終更新メモや保守対象名など、コード外の小さな記録ズレが独立監査で残った

次回から変えること:
- 既存の仕組みを作り直す場合は、最初から「v2横作成 -> 独立レビュー -> 人間確認 -> 現行入口差し替え -> 置換後監査」の5段階に分ける
- 差し替え時は旧版を `*_v1_legacy.*` として残す
- 置換後は、現行入口とv2のSHA一致、実行権限、dry-run、本番不変、影環境比較を必須証拠にする
- 仕様書、台帳、レビュー記録の文言ズレも完成判定の対象にする

テンプレ改訂が必要か:
必要。

改訂候補:
`AI開発フロー_標準テンプレ.md` に「既存仕組みの作り直し/差し替えルール」を追加する。

---

## 実案件2: mask-slurs CLI v2 横作成

### 案件分類

分類:
通常

理由:
- creative-thread-gen用のオンデマンドCLIで、外部送信、認証、常駐化、launchd登録を含まない
- ただしYouTube制作の安全ゲートに関わるため、単なる軽微変更ではなくテスト付きで扱う
- 現行入口の差し替えはまだ行わず、v2を横に作るだけに限定する

### Goal固定

現行 `mask_slurs.py` を消さず、同じ `qa_check.py` の `SLUR_RE` を単一ソースとして使う、より安全な `mask_slurs_v2.py` を横に作る。

### 禁止・制限

- 現行 `mask_slurs.py` を上書きしない
- 実コーパスを書き換えない
- launchd / cron / 常駐化をしない
- 外部送信しない
- 差し替えは人間確認後の別工程にする

### 実施内容

- `mask_slurs_v2.py` を横に作成
- `test_mask_slurs_v2.py` を作成
- `mask-slurs-cli-v2.md` を作成
- `導入済み.md` にv2検証候補として追記

### 要件・テスト・証拠対応

| 要件ID | 要件 | 証拠 | 状態 |
|---|---|---|---|
| REQ-M001 | 現行を消さずv2を横に作る | `mask_slurs.py` はそのまま、`mask_slurs_v2.py` を追加 | PASS |
| REQ-M002 | `qa_check.py` の `SLUR_RE` を単一ソースにする | v2が `qa_check.py` から `SLUR_RE` を読み込む | PASS |
| REQ-M003 | 標準入力/標準出力の基本動作を維持する | `test_stdin_stdout_masks_slurs_and_preserves_allowed_context` | PASS |
| REQ-M004 | 旧版との基本出力一致を確認する | `test_v2_matches_v1_stdout_for_basic_stdin_case` | PASS |
| REQ-M005 | ファイル出力を安全に扱う | 一時ファイル経由の保存、同一入出力テスト | PASS |
| REQ-M006 | dry-runは出力ファイルを書かない | `test_dry_run_does_not_write_output_file` | PASS |
| REQ-M007 | 欠落・危険入力はfail-closeする | 入力欠落、出力先欠落、`qa_check.py` 欠落、空置換文字のテスト | PASS |
| REQ-M008 | 直接実行できる | v2に実行権限、`test_direct_execution_help` | PASS |
| REQ-M009 | 旧版の import API を維持する | `test_backward_compatible_mask_import_api` | PASS |
| REQ-M010 | `--replacement` をリテラル扱いにする | `test_replacement_is_literal_not_regex_template` | PASS |
| REQ-M011 | 置換文字自体が蔑称なら止める | `test_slur_replacement_fails_close` | PASS |
| REQ-M012 | 差し替え後も旧版比較が空振りしない | `mask_slurs_v1_legacy.py` があればそれを旧版比較先にする | PASS |
| REQ-M013 | 既存出力ファイルの権限を維持する | `test_existing_output_mode_is_preserved` | PASS |

### 実行証拠

- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m py_compile ...`: 終了コード0
- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m unittest "00_システム/20_Agent_Portable/codex-skills-ready/creative-thread-gen/test_mask_slurs_v2.py"`: 15 tests OK
- `git diff --check -- ...mask_slurs_v2.py ...test_mask_slurs_v2.py ...mask-slurs-cli-v2.md`: 終了コード0
- `rg -n "mask_slurs|from .*mask|import .*mask" ...`: Vault内に `mask(text)` の直接import利用は見つからず。ただしv2に互換APIを追加済み

### 完成判定

判定:
v2検証候補として完成。現行差し替えは未実施。

理由:
- 現行入口を上書きせず、横にv2を作れた
- 旧版との基本出力一致を確認した
- テスト、構文チェック、差分チェックが通った
- 実コーパス、本番自動化、外部送信、認証、常駐化に触れていない

### 独立レビュー1

判定:
FAIL

指摘:
- 旧版の import API `mask(text)` がv2になかった
- `--replacement` が正規表現置換文字列として解釈される可能性があった
- 差し替え後、旧版比較テストが `mask_slurs.py` と `mask_slurs_v2.py` の比較になり、空振りする可能性があった
- 後回し候補として、既存出力ファイルの権限維持と `--replacement` 使用例の不足があった

対応:
- `mask(text)` 互換APIを追加
- `pattern.sub(lambda _match: replacement, text)` に変更し、置換文字をリテラル扱いにした
- 置換文字自体が `SLUR_RE` に当たる場合は終了コード2で止める
- テストの旧版比較先を、`mask_slurs_v1_legacy.py` があればそちらへ切り替える構造にした
- 既存出力ファイルの権限維持とテストを追加
- `--replacement` 使用例と安全設計を仕様書へ追記

再検証:
- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m py_compile ...`: 終了コード0
- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m unittest "00_システム/20_Agent_Portable/codex-skills-ready/creative-thread-gen/test_mask_slurs_v2.py"`: 15 tests OK
- `git diff --check -- ...mask_slurs_v2.py ...test_mask_slurs_v2.py ...mask-slurs-cli-v2.md`: 終了コード0

### 独立レビュー2

判定:
PASS

確認結果:
- 前回FAILの3点は解消済み
- 旧版 import API `mask(text)` はv2に追加済み
- `--replacement` は正規表現置換ではなくリテラル扱いに修正済み
- 差し替え後の互換テスト空振り対策として、`mask_slurs_v1_legacy.py` があれば旧版比較先にする構造になっている
- 15 tests OK、`git diff --check` OK

残注意（レビュー時点）:
レビュー時点では `mask_slurs_v1_legacy.py` はまだ存在しなかった。これは現行未差し替えのため自然な状態。差し替え時は、先に `mask_slurs.py` を `mask_slurs_v1_legacy.py` として保存してから、現行入口をv2へ切り替える。

### 次工程（レビュー時点）

再レビューを通したうえで、人間確認後に `mask_slurs.py` を `mask_slurs_v1_legacy.py` として保存し、現行入口をv2へ切り替える。

この工程は後続の「mask-slurs CLI v2 正式差し替え」で完了。

---

## 実案件2: mask-slurs CLI v2 正式差し替え

### Goal固定

旧版を `mask_slurs_v1_legacy.py` として残したうえで、現行入口 `mask_slurs.py` を同等以上の安全性を持つv2実装へ差し替える。

### 禁止・制限

- 旧版を削除しない
- 実コーパスを書き換えない
- launchd / cron / 常駐化をしない
- 外部送信しない

### 実施内容

- 旧版 `mask_slurs.py` を `mask_slurs_v1_legacy.py` として保存
- `mask_slurs_v2.py` を `mask_slurs.py` にコピー
- テストに、現行入口 `mask_slurs.py` とv2のSHA一致確認を追加
- 仕様書と台帳を「現行差し替え済み」に更新

### 要件・テスト・証拠対応

| 要件ID | 要件 | 証拠 | 状態 |
|---|---|---|---|
| REQ-MR001 | 旧版を残す | `mask_slurs_v1_legacy.py` を作成、SHA256 `0dd23dadb9a3aba210ed49b7749cafa53df322c7b329ad39e902840512c88520` | PASS |
| REQ-MR002 | 現行入口をv2実装へ差し替える | `mask_slurs.py` と `mask_slurs_v2.py` のSHA256が同一 `da7a8c2ca3251024398f6ba8e9a910a855b7457a9b93933fc76addf31ea3db47` | PASS |
| REQ-MR003 | 実行権限を確認する | `ls -l` で現行入口とv2が `-rwxr-xr-x` | PASS |
| REQ-MR004 | 置換後テストが通る | `test_mask_slurs_v2.py`: 16 tests OK | PASS |
| REQ-MR005 | 構文チェックが通る | `PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m py_compile ...`: 終了コード0 | PASS |
| REQ-MR006 | 仕様書と台帳を差し替え済みに更新する | `mask-slurs-cli-v2.md` と `導入済み.md` を更新 | PASS |
| REQ-MR007 | 旧版・現行入口・v2の影環境出力が一致する | `/tmp/codex-mask-slurs-audit` で3出力SHA256が同一 `87b84b1da3efefc5de18decb42aeaf1471f915424794b4e37149a17c6028de3b` | PASS |

### 実行証拠

- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m unittest "00_システム/20_Agent_Portable/codex-skills-ready/creative-thread-gen/test_mask_slurs_v2.py"`: 16 tests OK
- `env PYTHONPYCACHEPREFIX=/tmp/codex-pycache python3 -m py_compile ...`: 終了コード0
- 現行入口/v2 SHA256: `da7a8c2ca3251024398f6ba8e9a910a855b7457a9b93933fc76addf31ea3db47`
- 旧版 SHA256: `0dd23dadb9a3aba210ed49b7749cafa53df322c7b329ad39e902840512c88520`
- 影環境比較: 旧版・現行入口・v2の出力SHA256が同一 `87b84b1da3efefc5de18decb42aeaf1471f915424794b4e37149a17c6028de3b`
- 影環境比較出力: `特亜` と `鬼畜米英` は `【NG】` 化、`支那そば` は維持

### 完成判定

判定:
完成

理由:
- 旧版を保存した
- 現行入口とv2の内容一致を確認した
- テスト、構文チェック、仕様書、台帳更新が通った
- 実コーパス、本番自動化、外部送信、認証、常駐化に触れていない

### 置換後独立監査

判定:
PASS

確認結果:
- `mask_slurs.py` と `mask_slurs_v2.py` はSHA256一致、差分なし
- 旧版は `mask_slurs_v1_legacy.py` として残存
- テストは `CURRENT_SCRIPT` / `LEGACY_SCRIPT` / `V2_SCRIPT` を分け、差し替え後も空振りしにくい構造
- 仕様書は「完成 / 現行差し替え済み」と明記
- 台帳は `mask-slurs CLI` をv2実装へ差し替え済み、`mask-slurs CLI v2` を現行CLIと同一内容として記録
- レビュー記録内の「現行差し替えは未実施」は横作成フェーズの履歴として読める状態

最終判定:
完成
